# 🔍 Проверка доступа к боту

## Проблема: страница не открывается

Нужно проверить несколько вещей.

---

## Шаг 1: Проверить, что бот запущен и слушает порт

На сервере выполните:

```bash
# Проверить процессы
ps aux | grep "python3 main.py"

# Проверить, слушает ли порт 5001
netstat -tlnp | grep 5001
```

или

```bash
ss -tlnp | grep 5001
```

Должен быть процесс, слушающий порт 5001.

---

## Шаг 2: Проверить файрвол

```bash
# Проверить правила файрвола
ufw status
```

Если файрвол активен, нужно открыть порт:

```bash
ufw allow 5001
```

---

## Шаг 3: Попробовать доступ по IP

Попробуйте открыть в браузере:

```
http://212.67.11.50:5001
```

Если работает по IP, но не работает по домену - проблема в настройках DNS/поддомена.

---

## Шаг 4: Проверить настройки поддомена

В панели Beget:
1. Откройте настройки поддомена `reklama.ятута.рф`
2. Проверьте:
   - Куда направлен поддомен?
   - Есть ли настройки порта?
   - Нужно ли настроить реверс-прокси?

---

## Шаг 5: Настроить реверс-прокси (если нужно)

Если поддомен должен работать без указания порта, нужно настроить реверс-прокси.

В настройках поддомена найдите:
- "Прокси" или "Reverse Proxy"
- Укажите: `http://127.0.0.1:5001` или `http://localhost:5001`

---

## Альтернатива: Использовать другой порт (80 или 443)

Если нужно, чтобы работало без указания порта, можно:
1. Изменить порт на 80 (HTTP) или 443 (HTTPS)
2. Но для этого нужны права root и освободить порт

---

## Быстрая проверка:

Выполните на сервере:

```bash
# 1. Проверить, запущен ли бот
ps aux | grep "python3 main.py"

# 2. Проверить порт
netstat -tlnp | grep 5001

# 3. Проверить файрвол
ufw status
```

Пришлите вывод - помогу разобраться!


