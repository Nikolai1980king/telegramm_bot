# 🔍 Проверка страницы входа

## Проблема: страница входа не открывается

Возможные причины:
1. Файл `login.html` не загружен на сервер
2. Бот не перезапущен с новым кодом
3. Пароль не установлен (тогда редирект на главную)

---

## Проверка на сервере:

### 1. Проверить, есть ли файл login.html:

```bash
ls -la /opt/reklama_bot/templates/login.html
```

Если файла нет - нужно загрузить.

### 2. Проверить, что бот перезапущен с новым кодом:

```bash
# Остановить старый бот
pkill -f "python3 main.py"

# Проверить версию main.py (должна быть с защитой)
grep "login_required" /opt/reklama_bot/main.py
```

### 3. Попробовать открыть напрямую:

```
http://212.67.11.50:5001/login
```

---

## Решение:

### 1. Загрузить файл login.html на сервер:

Через файловый менеджер Beget:
- Откройте `/opt/reklama_bot/templates/`
- Загрузите файл `login.html`

### 2. Перезапустить бота:

```bash
cd /opt/reklama_bot
pkill -f "python3 main.py"
screen -S reklama_bot
python3 main.py
# Ctrl+A, затем D
```

### 3. Попробовать открыть:

```
http://212.67.11.50:5001/login
```

---

## Как работает защита:

1. **Если пароль НЕ установлен:**
   - Страница `/login` покажет сообщение "Пароль не установлен"
   - Страница `/` доступна без пароля

2. **Если пароль установлен:**
   - Страница `/` редиректит на `/login`
   - Страница `/login` требует ввода пароля
   - После ввода правильного пароля - доступ к панели

---

## Установка пароля:

1. Откройте: `http://212.67.11.50:5001` (если пароль не установлен - откроется сразу)
2. В разделе "🔒 Защита веб-интерфейса" введите пароль
3. Сохраните настройки
4. При следующем открытии потребуется ввести пароль


