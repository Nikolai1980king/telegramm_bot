# 🔍 Диагностика проблемы с отправкой сообщений

## Команды для проверки на сервере:

### 1. Проверить, запущен ли бот:
```bash
ps aux | grep "python3 main.py"
```

### 2. Проверить логи (если запущен через nohup):
```bash
cd /opt/reklama_bot
tail -50 bot.log
```

### 3. Проверить логи (если запущен через systemd):
```bash
journalctl -u reklama_bot -n 50 --no-pager
```

### 4. Проверить конфигурацию:
```bash
cd /opt/reklama_bot
cat bot_config.json
```

### 5. Проверить порт и веб-интерфейс:
```bash
netstat -tlnp | grep 5001
```

---

## Возможные проблемы:

1. **Бот не запущен** - запустите заново
2. **Ошибка авторизации** - проверьте session файл
3. **Неправильное расписание** - проверьте настройки schedule
4. **Интервал еще не прошел** - проверьте время последней отправки
5. **Ошибка отправки** - проверьте логи на ошибки

---

## Быстрая диагностика:

Выполните эти команды на сервере и пришлите вывод.

