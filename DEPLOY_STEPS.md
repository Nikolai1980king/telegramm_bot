# Пошаговая инструкция: Размещение бота на удаленном сервере

## 📋 Подготовка (локально)

### Шаг 1: Первая авторизация (создание session файла)

**ВАЖНО:** Это нужно сделать ДО загрузки на сервер!

1. Откройте терминал в папке проекта:
   ```bash
   cd /home/nikolai/PycharmProjects/telegramm_bot
   ```

2. Установите зависимости (если еще не установлены):
   ```bash
   pip install -r requirements.txt
   ```

3. Запустите бота один раз для авторизации:
   ```bash
   python main.py
   ```

4. При первом запуске:
   - Введите код из Telegram (придет в приложение Telegram)
   - После успешной авторизации появятся файлы:
     - `session_name.session`
     - `session_name.session-journal` (может отсутствовать)

5. **Остановите бота** (Ctrl+C) - авторизация завершена!

---

## 🚀 Загрузка на сервер

### Шаг 2: Подготовка файлов для загрузки

Файлы, которые нужно загрузить на сервер:

```
telegramm_bot/
├── main.py                    ✅ Обязательно
├── requirements.txt          ✅ Обязательно
├── bot_config.json          ✅ Обязательно
├── session_name.session      ✅ ОБЯЗАТЕЛЬНО (после авторизации!)
├── session_name.session-journal  ✅ Если есть
├── templates/
│   └── index.html           ✅ Обязательно
└── image/
    └── cart_1.png           ✅ Обязательно
```

### Шаг 3: Создание поддомена в панели Beget

1. Войдите в панель управления Beget: https://cp.beget.com

2. Перейдите в раздел **"Домены"** → **"Поддомены"**

3. Нажмите **"Создать поддомен"**

4. Заполните:
   - **Имя поддомена**: например `reklama` или `bot2`
   - **Домен**: выберите ваш основной домен
   - **Путь**: `/home/uXXXXXX/reklama_bot` (замените uXXXXXX на ваш логин)
   - **Тип**: выберите "Директория" или оставьте по умолчанию

5. Нажмите **"Создать"**

6. Запомните полный адрес поддомена: `reklama.yourdomain.com`

---

### Шаг 4: Создание папки на сервере

**Вариант A: Через файловый менеджер Beget**

1. В панели Beget перейдите в **"Файловый менеджер"**
2. Создайте папку `reklama_bot` в корне вашего аккаунта
3. Или используйте путь, указанный при создании поддомена

**Вариант B: Через FTP/SFTP**

1. Подключитесь к серверу через FTP клиент (FileZilla, WinSCP и т.д.)
2. Создайте папку `reklama_bot` в корневой директории

**Путь на сервере будет примерно таким:**
```
/home/u1234567/reklama_bot/
```
(где u1234567 - ваш логин в Beget)

---

### Шаг 5: Загрузка файлов на сервер

**Через FTP/SFTP (FileZilla, WinSCP):**

1. Подключитесь к серверу через FTP
2. Перейдите в папку `reklama_bot` на сервере
3. Загрузите файлы:
   - `main.py`
   - `requirements.txt`
   - `bot_config.json`
   - `session_name.session` ⚠️ **ВАЖНО!**
   - `session_name.session-journal` (если есть)
   - Папку `templates/` с файлом `index.html`
   - Папку `image/` с файлом `cart_1.png`

**Через файловый менеджер Beget:**

1. В панели Beget → "Файловый менеджер"
2. Перейдите в папку `reklama_bot`
3. Загрузите файлы через веб-интерфейс

---

### Шаг 6: Настройка путей в bot_config.json

После загрузки на сервер, отредактируйте `bot_config.json` на сервере:

**Через файловый менеджер Beget:**
1. Откройте файл `bot_config.json`
2. Убедитесь, что пути относительные:
   ```json
   {
       "photo_path": "image/cart_1.png",
       "session_file": "session_name"
   }
   ```

**Или через веб-интерфейс бота** (после запуска):
- Зайдите на поддомен и настройте через форму

---

### Шаг 7: Установка зависимостей на сервере

**Вариант A: Через панель Beget (Python приложения)**

1. В панели Beget найдите раздел **"Python"** или **"Python приложения"**
2. Создайте новое Python приложение:
   - **Путь к проекту**: `/home/uXXXXXX/reklama_bot`
   - **Файл запуска**: `main.py`
   - **Поддомен**: выберите созданный поддомен
3. В настройках приложения найдите опцию установки зависимостей
4. Или используйте команду (если есть доступ к терминалу):
   ```bash
   pip3 install --user -r requirements.txt
   ```

**Вариант B: Через SSH (если доступен)**

1. Подключитесь к серверу по SSH
2. Перейдите в папку проекта:
   ```bash
   cd /home/uXXXXXX/reklama_bot
   ```
3. Установите зависимости:
   ```bash
   pip3 install --user -r requirements.txt
   ```

---

### Шаг 8: Настройка Python приложения в Beget

1. В панели Beget → **"Python"** → **"Python приложения"**

2. Создайте новое приложение или отредактируйте существующее:

   **Основные настройки:**
   - **Название**: `reklama_bot` (любое удобное)
   - **Путь к проекту**: `/home/uXXXXXX/reklama_bot`
   - **Файл запуска**: `main.py`
   - **Домен/поддомен**: выберите созданный поддомен
   - **Порт**: (обычно настраивается автоматически)

3. **Важно:** Убедитесь, что приложение настроено на запуск Flask приложения

4. Сохраните настройки

---

### Шаг 9: Проверка прав доступа к файлам

Убедитесь, что файлы имеют правильные права:

**Через SSH:**
```bash
cd /home/uXXXXXX/reklama_bot
chmod 644 *.py *.json *.txt
chmod 644 templates/*.html
chmod 644 image/*
chmod 600 session_name.session  # Session файл - только чтение/запись для владельца
```

**Через файловый менеджер Beget:**
- Установите права 644 для всех файлов
- Для session файла: 600

---

### Шаг 10: Запуск и проверка

1. **Запустите Python приложение** в панели Beget

2. **Откройте в браузере** ваш поддомен:
   ```
   http://reklama.yourdomain.com
   ```
   или
   ```
   https://reklama.yourdomain.com
   ```

3. **Проверьте веб-интерфейс:**
   - Должна открыться страница управления ботом
   - Проверьте настройки
   - Запустите бота через кнопку "Запустить бота"

4. **Проверьте логи** в панели Beget на наличие ошибок

---

## 🔧 Решение проблем

### Бот не запускается

1. **Проверьте логи** в панели Beget
2. **Убедитесь, что session файл загружен** на сервер
3. **Проверьте пути** в `bot_config.json`
4. **Проверьте права доступа** к файлам

### Ошибка авторизации

- Убедитесь, что `session_name.session` загружен на сервер
- Проверьте права доступа (должен быть 600)
- Если session файл не работает, создайте новый локально и загрузите снова

### Поддомен не открывается

1. Проверьте настройки поддомена в панели
2. Убедитесь, что Python приложение привязано к поддомену
3. Проверьте DNS (обычно настраивается автоматически)
4. Подождите 5-10 минут для применения изменений DNS

### Зависимости не устанавливаются

1. Проверьте версию Python (должна быть 3.6+)
2. Попробуйте установить вручную через SSH:
   ```bash
   pip3 install --user telethon Flask cryptg
   ```

---

## ✅ Чеклист перед запуском

- [ ] Session файл создан локально и загружен на сервер
- [ ] Все файлы загружены в правильную папку
- [ ] Пути в `bot_config.json` относительные
- [ ] Зависимости установлены на сервере
- [ ] Поддомен создан и настроен
- [ ] Python приложение создано и привязано к поддомену
- [ ] Права доступа к файлам установлены правильно
- [ ] Веб-интерфейс открывается по адресу поддомена

---

## 📞 Дополнительная помощь

Если что-то не работает:
1. Проверьте логи в панели Beget
2. Убедитесь, что все шаги выполнены
3. Проверьте документацию Beget по Python приложениям


