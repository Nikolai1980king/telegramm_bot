# 📤 Загрузка обновленного main.py на сервер

## Что изменилось:

Добавлена автоматическая перезагрузка конфига - теперь изменения применяются без перезапуска бота!

---

## Способы загрузки:

### Вариант 1: Через scp (с вашего компьютера)

```bash
cd ~/PycharmProjects/telegramm_bot
scp main.py root@212.67.11.50:/opt/reklama_bot/
```

Если запросит пароль - введите пароль от сервера.

---

### Вариант 2: Через SFTP/FTP клиент

1. Подключитесь к серверу через FileZilla или другой FTP клиент
2. Скопируйте файл `main.py` из локальной папки
3. Загрузите в `/opt/reklama_bot/` на сервере (замените старый файл)

---

### Вариант 3: Через веб-интерфейс хостинга

Если у Beget есть файловый менеджер:
1. Зайдите в панель Beget
2. Откройте файловый менеджер
3. Перейдите в `/opt/reklama_bot/`
4. Загрузите новый `main.py`

---

### Вариант 4: Создать файл на сервере вручную (если есть доступ к редактору)

Через SSH:
```bash
ssh root@212.67.11.50
cd /opt/reklama_bot
nano main.py
# Скопируйте содержимое нового файла
# Сохраните: Ctrl+O, Enter, Ctrl+X
```

---

## После загрузки файла:

```bash
cd /opt/reklama_bot

# 1. Создать резервную копию (на всякий случай)
cp main.py main.py.backup_$(date +%Y%m%d)

# 2. Проверить синтаксис
python3 -m py_compile main.py && echo "✅ Синтаксис правильный!"

# 3. Перезапустить бот
pkill -9 -f "python3 main.py"
sleep 2
nohup python3 main.py > bot.log 2>&1 &

# 4. Проверить логи
sleep 5
tail -30 bot.log
```

---

## ✅ Что изменилось в коде:

1. **Автоперезагрузка конфига** - бот загружает конфиг перед каждым циклом
2. **Автоматическое применение изменений** - не нужно перезапускать бота
3. **Проверка is_running** - бот останавливается, если его остановили через веб-интерфейс

