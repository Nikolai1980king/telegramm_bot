# 🔧 Исправление порта 5000

## Проблема:
Порт 5000 занят, но в коде используется 5000, а нужно 5001.

## Решение:

### Шаг 1: Найти процесс, который занимает порт 5000

```bash
lsof -i :5000
# или
netstat -tlnp | grep 5000
```

### Шаг 2: Остановить процесс на порту 5000 (если это старый бот)

```bash
# Найти PID процесса
fuser -k 5000/tcp
# или
kill -9 $(lsof -t -i:5000)
```

### Шаг 3: Изменить порт в коде на 5001

```bash
cd /opt/reklama_bot
sed -i 's/port=5000/port=5001/g' main.py
```

### Шаг 4: Перезапустить бот

```bash
pkill -9 -f "python3 main.py"
sleep 2
nohup python3 main.py > bot.log 2>&1 &
sleep 5
tail -30 bot.log
```

---

## ⚡ Или всё одной командой:

```bash
cd /opt/reklama_bot && fuser -k 5000/tcp 2>/dev/null; sed -i 's/port=5000/port=5001/g' main.py && pkill -9 -f "python3 main.py" && sleep 2 && nohup python3 main.py > bot.log 2>&1 & sleep 5 && tail -30 bot.log
```

