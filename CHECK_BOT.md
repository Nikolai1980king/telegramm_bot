# 🔍 Проверка работы бота

## Команды для проверки на сервере:

### 1. Проверить, запущен ли процесс бота:

```bash
ps aux | grep python
```

Должен быть виден процесс `python3 main.py`

### 2. Проверить screen сессии:

```bash
screen -ls
```

Должна быть сессия `reklama_bot`

### 3. Вернуться к сессии screen (если запускали через screen):

```bash
screen -r reklama_bot
```

Посмотрите, что там выводится - есть ли ошибки?

### 4. Проверить логи (если запускали через nohup):

```bash
tail -f /opt/reklama_bot/bot.log
```

### 5. Проверить, слушает ли Flask порт:

```bash
netstat -tlnp | grep python
```

или

```bash
ss -tlnp | grep python
```

Должен быть процесс, слушающий порт (обычно 5000)

### 6. Проверить файлы и права доступа:

```bash
cd /opt/reklama_bot
ls -la
```

Проверьте, что:
- `session_name.session` существует
- Все файлы имеют правильные права

### 7. Попробовать запустить бота вручную (для просмотра ошибок):

```bash
cd /opt/reklama_bot
python3 main.py
```

Посмотрите, какие ошибки выводятся.

---

## Возможные проблемы:

### Проблема 1: Зависимости не установлены

**Решение:**
```bash
pip3 install --user --break-system-packages -r requirements.txt
```

### Проблема 2: Session файл не найден или не авторизован

**Решение:**
- Убедитесь, что `session_name.session` загружен на сервер
- Проверьте права доступа: `chmod 600 session_name.session`

### Проблема 3: Неправильные пути в конфиге

**Решение:**
```bash
cd /opt/reklama_bot
cat bot_config.json
```

Проверьте, что пути относительные или правильные абсолютные.

### Проблема 4: Порт занят или не доступен

**Решение:**
- Проверьте, не запущен ли уже другой процесс на порту 5000
- Возможно, нужно изменить порт в `main.py`

### Проблема 5: Flask не может привязаться к 0.0.0.0:5000

**Решение:**
- Проверьте настройки файрвола
- Возможно, нужно использовать другой порт или хост

---

## Быстрая диагностика:

Выполните на сервере:

```bash
cd /opt/reklama_bot
python3 main.py
```

И посмотрите, какие ошибки выводятся. Пришлите вывод - помогу исправить!


