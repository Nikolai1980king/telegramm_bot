# 🔧 Остановить все процессы Python

## Проблема:
Старый процесс все еще работает и использует порт 5000.

## Решение:

```bash
cd /opt/reklama_bot

# 1. Найти ВСЕ процессы Python, связанные с main.py
ps aux | grep python | grep main

# 2. Найти процесс на порту 5000
lsof -i :5000

# 3. Найти процесс на порту 5001
lsof -i :5001

# 4. Убить ВСЕ процессы Python main.py
pkill -9 -f "main.py"
pkill -9 -f "python.*main"

# 5. Убить процесс на порту 5000
fuser -k 5000/tcp 2>/dev/null
kill -9 $(lsof -t -i:5000) 2>/dev/null

# 6. Подождать
sleep 3

# 7. Проверить, что все остановлено
ps aux | grep python | grep main
lsof -i :5000
lsof -i :5001

# 8. Проверить порт в коде (должен быть 5001)
grep "port=" main.py

# 9. Запустить заново
nohup python3 main.py > bot.log 2>&1 &

# 10. Подождать и проверить
sleep 5
tail -40 bot.log
netstat -tlnp | grep 5001
```

