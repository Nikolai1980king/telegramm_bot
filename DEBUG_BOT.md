# 🔍 Диагностика проблемы с ботом

## Проблемы:
1. Кнопка "Запустить бота" не реагирует
2. Бот не присылает сообщения

---

## Шаг 1: Проверить логи бота

На сервере выполните:

```bash
# Вернуться к screen сессии
screen -r reklama_bot
```

Посмотрите, что выводится в терминале. Есть ли ошибки?

Или если через nohup:
```bash
tail -f /opt/reklama_bot/bot.log
```

---

## Шаг 2: Проверить конфиг

```bash
cd /opt/reklama_bot
cat bot_config.json
```

Проверьте:
- Правильные ли получатели?
- Правильный ли путь к изображению?
- Правильный ли session файл?

---

## Шаг 3: Проверить session файл

```bash
ls -la /opt/reklama_bot/session_name.session
```

Убедитесь, что файл существует и имеет правильные права (600).

---

## Шаг 4: Проверить изображение

```bash
ls -la /opt/reklama_bot/image/
```

Убедитесь, что файл `cart_1.png` существует.

---

## Шаг 5: Проверить, запускается ли бот вручную

В screen сессии попробуйте остановить бот (Ctrl+C) и запустить заново:

```bash
cd /opt/reklama_bot
python3 main.py
```

Посмотрите, какие ошибки выводятся при запуске бота через веб-интерфейс.

---

## Возможные проблемы:

### Проблема 1: Session файл не авторизован

**Решение:** Нужно запустить бота локально для авторизации, затем загрузить session файл на сервер.

### Проблема 2: Неправильные получатели

**Решение:** Проверьте формат получателей в конфиге - должны быть `@username` или ID.

### Проблема 3: Изображение не найдено

**Решение:** Проверьте путь к изображению в конфиге.

### Проблема 4: Ошибка при отправке

**Решение:** Проверьте логи - там будут детали ошибки.

---

## Быстрая проверка:

Выполните на сервере:

```bash
# 1. Вернуться к screen
screen -r reklama_bot

# 2. Посмотреть вывод (если бот запущен, нажмите Ctrl+C чтобы остановить)

# 3. Запустить вручную и посмотреть ошибки
cd /opt/reklama_bot
python3 main.py
```

Пришлите вывод - увижу ошибки!


