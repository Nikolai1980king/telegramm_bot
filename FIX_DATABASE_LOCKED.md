# 🔧 Исправление ошибки "database is locked"

## Проблема:
```
sqlite3.OperationalError: database is locked
```

Это происходит когда несколько процессов пытаются использовать один session файл.

## Решение:

### Шаг 1: Остановить все процессы бота

```bash
cd /opt/reklama_bot
pkill -9 -f "python3 main.py"
ps aux | grep "python3 main.py" | grep -v grep
```

Если процесс еще есть - убить вручную:
```bash
kill -9 <PID>
```

### Шаг 2: Проверить, нет ли заблокированных session файлов

```bash
ls -lh /opt/reklama_bot/*.session*
fuser /opt/reklama_bot/*.session* 2>/dev/null || echo "Файлы не заблокированы"
```

### Шаг 3: Удалить lock файлы (если есть)

```bash
rm -f /opt/reklama_bot/*.session-journal
rm -f /opt/reklama_bot/*.session-wal
```

### Шаг 4: Перезапустить бота

```bash
cd /opt/reklama_bot
nohup python3 main.py > bot.log 2>&1 &
```

### Шаг 5: Проверить логи

```bash
sleep 3
tail -20 bot.log
```

### Шаг 6: Проверить, что бот запущен

```bash
ps aux | grep "python3 main.py" | grep -v grep
```

---

## ⚠️ Важно:

После перезапуска подождите минуту и проверьте логи - бот должен начать отправлять сообщения.

Если ошибка повторится, возможно session файл поврежден. В этом случае нужно пересоздать его локально.

