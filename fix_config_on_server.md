# 🔧 Исправление конфигурации на сервере

## Проблема: Отсутствуют поля расписания в конфиге

Если в конфиге на сервере отсутствуют новые поля (`schedule_type`, `schedule_days`, и т.д.), бот может работать некорректно.

## Решение:

### Вариант 1: Через веб-интерфейс (рекомендуется)

1. Откройте веб-интерфейс бота: http://212.67.11.50:5001
2. Зайдите в настройки
3. Убедитесь, что выбран режим "Интервал" или "Расписание"
4. Сохраните настройки

### Вариант 2: Исправить конфиг вручную на сервере

Выполните на сервере:

```bash
cd /opt/reklama_bot

# Создать резервную копию
cp bot_config.json bot_config.json.backup

# Проверить текущий конфиг
cat bot_config.json

# Если отсутствуют поля расписания, добавьте их через Python:
python3 << 'PYTHON'
import json
import os

config_file = '/opt/reklama_bot/bot_config.json'

# Загрузить конфиг
with open(config_file, 'r') as f:
    config = json.load(f)

# Добавить недостающие поля, если их нет
if 'schedule_type' not in config:
    config['schedule_type'] = 'interval'
if 'schedule_days' not in config:
    config['schedule_days'] = [0, 1, 2, 3, 4, 5, 6]
if 'schedule_hours' not in config:
    config['schedule_hours'] = [9, 12, 18]
if 'schedule_even_odd' not in config:
    config['schedule_even_odd'] = 'both'
if 'web_password' not in config:
    config['web_password'] = ''

# Сохранить
with open(config_file, 'w') as f:
    json.dump(config, f, indent=4, ensure_ascii=False)

print("✅ Конфиг обновлен!")
print(f"schedule_type: {config['schedule_type']}")
PYTHON

# Проверить результат
cat bot_config.json
```

### Вариант 3: Перезапустить бот (он сам добавит недостающие поля)

Бот автоматически добавит недостающие поля при следующей загрузке конфига. Просто перезапустите:

```bash
cd /opt/reklama_bot
pkill -f "python3 main.py"
nohup python3 main.py > bot.log 2>&1 &
```

---

## После исправления:

1. Проверьте логи:
   ```bash
   tail -f /opt/reklama_bot/bot.log
   ```

2. Проверьте, что бот работает:
   ```bash
   ps aux | grep "python3 main.py"
   ```

3. Проверьте веб-интерфейс:
   Откройте http://212.67.11.50:5001

