# ⚡ Быстрый автозапуск (через nohup)

## Простое решение без systemd

---

## 📋 Команды для запуска на сервере:

### Шаг 1: Подключитесь к серверу

```bash
ssh root@212.67.11.50
```

### Шаг 2: Остановите старый бот

```bash
cd /opt/reklama_bot
pkill -f "python3 main.py"
```

### Шаг 3: Запустите бота через nohup

```bash
cd /opt/reklama_bot
nohup python3 main.py > bot.log 2>&1 &
```

### Шаг 4: Проверьте, что бот запущен

```bash
ps aux | grep "python3 main.py"
```

Должна быть строка с процессом бота.

### Шаг 5: Посмотрите логи (опционально)

```bash
tail -f /opt/reklama_bot/bot.log
```

Для выхода: **Ctrl+C**

---

## 🔄 Управление:

### Остановить:
```bash
pkill -f "python3 main.py"
```

### Посмотреть логи:
```bash
tail -f /opt/reklama_bot/bot.log
```

### Проверить работу:
```bash
ps aux | grep "python3 main.py"
```

---

## ⚠️ Примечание:

`nohup` запускает процесс в фоне, но:
- ❌ Не перезапускается при сбоях
- ❌ Не запускается автоматически при перезагрузке сервера

Для более надежной работы используйте **systemd service** (см. `SETUP_AUTO_START.md`).

