# 🔄 Обновление бота на сервере

## Шаг 1: Остановить старый бот

На сервере выполните:

```bash
# Остановить все процессы бота
pkill -f "python3 main.py"

# Убить все screen сессии
pkill -f "screen.*reklama_bot"

# Проверить, что остановлено
ps aux | grep "python3 main.py"
```

---

## Шаг 2: Загрузить обновленные файлы

### Вариант A: Через файловый менеджер Beget

1. Откройте файловый менеджер в панели Beget
2. Перейдите в `/opt/reklama_bot/`
3. Загрузите обновленные файлы:
   - `main.py` (замените старый)
   - `templates/index.html` (замените старый)

### Вариант B: Через SCP (с локального компьютера)

```bash
# На локальном компьютере
scp main.py root@212.67.11.50:/opt/reklama_bot/
scp templates/index.html root@212.67.11.50:/opt/reklama_bot/templates/
```

---

## Шаг 3: Проверить порт

```bash
# Проверить, что порт 5001 свободен (или используйте другой)
netstat -tlnp | grep 5001
```

Если порт занят, измените в main.py на другой (например, 5002).

---

## Шаг 4: Перезапустить бота

```bash
cd /opt/reklama_bot
screen -S reklama_bot
python3 main.py
```

Затем **Ctrl+A**, затем **D**.

---

## Шаг 5: Проверить работу

1. Откройте веб-интерфейс: `http://212.67.11.50:5001`
2. Проверьте, что появился раздел "Настройки расписания отправки"
3. Настройте расписание и сохраните


