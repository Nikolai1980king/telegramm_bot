# 📦 Настройка репозитория на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Введите название репозитория (например: `telegramm_bot`)
3. Выберите **Public** или **Private** (рекомендуется Private, так как это бот)
4. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
5. Нажмите **Create repository**

---

## Шаг 2: Добавьте remote и запушьте код

После создания репозитория GitHub покажет команды. Выполните их:

```bash
cd ~/PycharmProjects/telegramm_bot

# Добавить remote (замените YOUR_REPO_NAME на название вашего репозитория)
git remote add origin https://github.com/Nikolai1980king/YOUR_REPO_NAME.git

# Переименовать ветку в main (опционально, но рекомендуется)
git branch -M main

# Запушить код
git push -u origin main
```

Если вы используете HTTPS, GitHub попросит ввести:
- **Username**: `Nikolai1980king`
- **Password**: Используйте **Personal Access Token** (не пароль!)

---

## 🔐 Создание Personal Access Token (если нужно)

Если GitHub просит пароль:

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **Generate new token** → **Generate new token (classic)**
3. Введите название: `telegramm_bot`
4. Выберите срок действия
5. Выберите права: **repo** (полный доступ к репозиториям)
6. Нажмите **Generate token**
7. **Скопируйте токен** (он показывается только один раз!)
8. Используйте этот токен как пароль при `git push`

---

## ✅ Альтернатива: SSH ключ (рекомендуется)

Если хотите избежать ввода пароля каждый раз:

### 1. Проверьте, есть ли SSH ключ:

```bash
ls -la ~/.ssh/id_rsa.pub
```

### 2. Если нет - создайте:

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
# Нажмите Enter для всех вопросов
```

### 3. Покажите публичный ключ:

```bash
cat ~/.ssh/id_rsa.pub
```

### 4. Добавьте ключ на GitHub:

1. Скопируйте вывод команды выше
2. Перейдите: https://github.com/settings/keys
3. Нажмите **New SSH key**
4. Вставьте ключ и сохраните

### 5. Используйте SSH URL:

```bash
git remote set-url origin git@github.com:Nikolai1980king/YOUR_REPO_NAME.git
git push -u origin main
```

---

## 🎯 Быстрая команда (если репозиторий уже создан)

```bash
cd ~/PycharmProjects/telegramm_bot
git remote add origin https://github.com/Nikolai1980king/telegramm_bot.git
git branch -M main
git push -u origin main
```

---

## 📋 Что НЕ попало в репозиторий:

Благодаря `.gitignore`, следующие файлы **не загружены**:
- ✅ `bot_config.json` - ваш конфиг с API ключами
- ✅ `session_name.session` - файл авторизации Telegram
- ✅ `*.log` - логи работы бота

Эти файлы нужно хранить в секрете!

