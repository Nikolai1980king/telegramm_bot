# 🔍 Диагностика проблемы запуска

## Проблема: бот не запускается, терминал пропал

Возможные причины:
1. Бот упал с ошибкой
2. Проблема со screen
3. Бот запустился, но сразу завершился

---

## Шаг 1: Проверить screen сессии

```bash
screen -ls
```

Если видите сессию `reklama_bot`, вернитесь к ней:
```bash
screen -r reklama_bot
```

---

## Шаг 2: Запустить бота БЕЗ screen (чтобы увидеть ошибки)

```bash
cd /opt/reklama_bot
python3 main.py
```

**Оставьте терминал открытым** - вы увидите все ошибки!

---

## Шаг 3: Проверить конфиг

```bash
cd /opt/reklama_bot
cat bot_config.json
```

Проверьте пути - они должны быть правильными.

---

## Шаг 4: Проверить session файл

```bash
ls -la /opt/reklama_bot/session_name.session
```

Убедитесь, что файл существует и имеет правильные права.

---

## Шаг 5: Проверить Python и зависимости

```bash
python3 --version
python3 -c "import telethon; import flask; print('OK')"
```

---

## Возможные проблемы:

### Проблема 1: Session файл не авторизован

**Решение:** Нужно запустить бота локально для авторизации, затем загрузить session файл.

### Проблема 2: Неправильные пути в конфиге

**Решение:** Проверьте `bot_config.json` - пути должны быть относительными или правильными абсолютными.

### Проблема 3: Порт занят

**Решение:** Измените порт в `main.py` или остановите процесс, занимающий порт 5000.

### Проблема 4: Ошибка импорта

**Решение:** Убедитесь, что все зависимости установлены.

---

## Быстрая проверка:

Выполните на сервере:

```bash
cd /opt/reklama_bot
python3 main.py 2>&1
```

Пришлите весь вывод - увижу ошибки!


