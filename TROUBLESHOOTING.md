# 🔧 Решение проблемы: сайт не открывается

## ❌ Проблема: "Не удаётся установить соединение с сайтом"

Это означает, что Flask приложение не запущено или не настроено правильно.

---

## 🔍 Диагностика проблемы

### 1. Проверьте, запущено ли приложение

**В панели Beget проверьте:**
- Есть ли раздел, где показываются запущенные процессы?
- Есть ли статус приложения (запущено/остановлено)?

### 2. Проверьте настройки поддомена

1. Откройте настройки поддомена `reklama.ятута.рф`
2. Проверьте:
   - Куда направлен поддомен?
   - Есть ли настройки типа приложения?
   - Указан ли путь `/opt/reklama_bot`?

---

## ✅ Решения

### Решение 1: Запуск через SSH (если доступен)

**Это самый надежный способ!**

1. **Проверьте доступ к SSH:**
   - В панели Beget найдите раздел **"SSH"** или **"Терминал"**
   - Или используйте SSH клиент (PuTTY, Terminal)

2. **Подключитесь к серверу:**
   ```bash
   ssh ваш_логин@сервер_beget
   ```

3. **Установите зависимости:**
   ```bash
   cd /opt/reklama_bot
   pip3 install --user -r requirements.txt
   ```

4. **Запустите бота в фоне:**
   ```bash
   cd /opt/reklama_bot
   nohup python3 main.py > bot.log 2>&1 &
   ```

   Или через screen (лучше):
   ```bash
   screen -S reklama_bot
   cd /opt/reklama_bot
   python3 main.py
   # Нажмите Ctrl+A, затем D для отключения
   ```

5. **Проверьте, что бот запущен:**
   ```bash
   ps aux | grep python
   # Должен быть процесс python3 main.py
   ```

6. **Проверьте логи:**
   ```bash
   tail -f /opt/reklama_bot/bot.log
   # или
   screen -r reklama_bot
   ```

---

### Решение 2: Настройка через .htaccess и mod_wsgi

Если Beget поддерживает mod_wsgi:

1. **Загрузите файл `wsgi.py` на сервер** в `/opt/reklama_bot/`

2. **Создайте файл `.htaccess`** в `/opt/reklama_bot/`:
   ```apache
   WSGIScriptAlias / /opt/reklama_bot/wsgi.py
   WSGIPythonPath /opt/reklama_bot

   <Directory /opt/reklama_bot>
       WSGIApplicationGroup %{GLOBAL}
       Require all granted
   </Directory>
   ```

3. **В настройках поддомена** укажите:
   - Тип: Apache/PHP (если нет Python)
   - Document Root: `/opt/reklama_bot`

---

### Решение 3: Обратиться в поддержку Beget

Если ничего не работает:

1. Напишите в поддержку Beget
2. Спросите: 
   - "Как запустить Flask приложение на поддомене?"
   - "Нужен ли специальный тариф для Python приложений?"
3. Укажите:
   - Поддомен: `reklama.ятута.рф`
   - Путь: `/opt/reklama_bot`
   - Файл: `main.py`

---

### Решение 4: Использовать другой порт

Возможно, нужно указать порт в URL:

- `http://reklama.ятута.рф:5000`
- `http://reklama.ятута.рф:8080`

Проверьте в настройках поддомена, какой порт назначен.

---

## 🎯 Рекомендуемый порядок действий:

1. **Проверьте доступ к SSH** - это самый надежный способ
2. Если SSH есть - запустите через SSH (Решение 1)
3. Если SSH нет - обратитесь в поддержку (Решение 3)
4. Параллельно попробуйте Решение 2 (через .htaccess)

---

## 📝 Что проверить прямо сейчас:

1. **Есть ли у вас доступ к SSH?**
   - Проверьте в панели Beget раздел "SSH"
   - Или попробуйте подключиться через терминал

2. **Что показывает настройки поддомена?**
   - Куда направлен поддомен?
   - Есть ли настройки типа приложения?

3. **Есть ли раздел "Процессы" или "Запущенные приложения"?**
   - Проверьте, запущено ли что-то

---

## ⚠️ Важно:

- Flask приложение должно быть **запущено постоянно**
- Если просто открыть файл через браузер - не сработает
- Нужен запущенный процесс Python, который слушает запросы


