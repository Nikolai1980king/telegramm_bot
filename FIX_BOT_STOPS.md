# 🔧 Проблема: Бот запускается, но потом останавливается

## Симптомы:
- В интерфейсе показывается "Работает"
- Когда выходите и возвращаетесь - бот "Остановлен"
- Происходит каждый раз

## Причины:

### 1. Бот падает сразу после запуска

Проверьте логи на сервере:

```bash
cd /opt/reklama_bot
tail -100 bot.log
```

Ищите ошибки:
- `❌ Требуется авторизация!` - проблема с session файлом
- `⚠️ Файл не найден` - проблема с путем к изображению
- `❌ Критическая ошибка` - другая ошибка
- `database is locked` - проблема с базой данных

---

### 2. Проблема с авторизацией

Если видите `❌ Требуется авторизация!`:

1. Проверьте session файл:
   ```bash
   ls -lh /opt/reklama_bot/*.session
   ```

2. Если файл маленький (< 1KB) - нужна авторизация:
   - Запустите бота локально один раз
   - Загрузите `session_name.session` на сервер

---

### 3. Проблема с файлом изображения

Если видите `⚠️ Файл не найден`:

1. Проверьте путь в конфиге:
   ```bash
   cat /opt/reklama_bot/bot_config.json | grep photo_path
   ```

2. Проверьте, существует ли файл:
   ```bash
   ls -lh /opt/reklama_bot/image/
   ```

3. Исправьте путь в веб-интерфейсе или конфиге

---

## Решение:

Я добавил синхронизацию статуса - теперь при загрузке страницы статус проверяется с реальным состоянием потока.

### Шаг 1: Загрузить обновленный файл

```bash
cd ~/PycharmProjects/telegramm_bot
scp main.py root@212.67.11.50:/opt/reklama_bot/
```

### Шаг 2: Перезапустить бот на сервере

```bash
cd /opt/reklama_bot
pkill -9 -f "python3 main.py"
sleep 2
nohup python3 main.py > bot.log 2>&1 &
sleep 5
tail -50 bot.log
```

---

## Диагностика:

### Команды для проверки:

```bash
cd /opt/reklama_bot

# 1. Посмотреть последние ошибки
tail -100 bot.log | grep -i "error\|❌\|⚠️"

# 2. Проверить процесс
ps aux | grep "python3 main.py" | grep -v grep

# 3. Проверить конфиг
cat bot_config.json | grep is_running

# 4. Проверить session файл
ls -lh *.session

# 5. Проверить файл изображения
ls -lh image/
```

---

## Что изменилось:

1. ✅ Синхронизация статуса - при загрузке страницы проверяется реальное состояние потока
2. ✅ Если поток не работает, статус автоматически обновляется на "Остановлен"

---

## Следующие шаги:

1. Проверьте логи, чтобы найти причину остановки
2. Загрузите обновленный `main.py`
3. Перезапустите бот
4. Если проблема продолжается - пришлите вывод `tail -100 bot.log`

