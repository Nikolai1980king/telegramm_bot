# 🔍 Пошаговая диагностика на сервере

## Выполните команды по порядку:

### Шаг 1: Перейти в папку бота
```bash
cd /opt/reklama_bot
```

### Шаг 2: Проверить, запущен ли бот
```bash
ps aux | grep "python3 main.py" | grep -v grep
```

### Шаг 3: Посмотреть логи
```bash
tail -50 bot.log
```

### Шаг 4: Проверить конфигурацию
```bash
cat bot_config.json
```

### Шаг 5: Проверить время сервера
```bash
date
```

### Шаг 6: Проверить файл изображения
```bash
ls -lh image/
```

---

## Или выполните все сразу:

```bash
cd /opt/reklama_bot && echo "=== ПРОЦЕСС ===" && ps aux | grep "python3 main.py" | grep -v grep && echo -e "\n=== ЛОГИ ===" && tail -50 bot.log && echo -e "\n=== КОНФИГ ===" && cat bot_config.json && echo -e "\n=== ВРЕМЯ ===" && date
```

