# 📤 Загрузка файлов в /opt/reklama_bot

## ✅ Папка создана: `/opt/reklama_bot`

Теперь нужно загрузить туда файлы проекта.

---

## 📦 Файлы для загрузки:

### 1. Основные файлы (в корень папки `/opt/reklama_bot/`):
- ✅ `main.py` (15KB) - основной файл бота
- ✅ `requirements.txt` (45 байт) - зависимости
- ✅ `bot_config.json` (659 байт) - конфигурация
- ✅ `session_name.session` (28KB) - **ВАЖНО!** Файл авторизации

### 2. Папка `templates/`:
- Создайте папку `templates` в `/opt/reklama_bot/`
- Загрузите туда: `templates/index.html`

### 3. Папка `image/`:
- Создайте папку `image` в `/opt/reklama_bot/`
- Загрузите туда: `image/cart_1.png`

---

## 📁 Итоговая структура должна быть:

```
/opt/reklama_bot/
├── main.py
├── requirements.txt
├── bot_config.json
├── session_name.session      ← ВАЖНО!
├── templates/
│   └── index.html
└── image/
    └── cart_1.png
```

---

## 🚀 Как загрузить через файловый менеджер:

### Шаг 1: Загрузить основные файлы
1. В правой панели откройте папку `/opt/reklama_bot`
2. Нажмите **"Загрузить Файлы"** в меню
3. Выберите файлы:
   - `main.py`
   - `requirements.txt`
   - `bot_config.json`
   - `session_name.session`
4. Загрузите их

### Шаг 2: Создать папку templates
1. В папке `/opt/reklama_bot` нажмите **"Новая папка"**
2. Назовите: `templates`
3. Откройте папку `templates`
4. Загрузите файл `index.html` из вашей локальной папки `templates/`

### Шаг 3: Создать папку image
1. Вернитесь в `/opt/reklama_bot`
2. Создайте папку `image`
3. Откройте папку `image`
4. Загрузите файл `cart_1.png` из вашей локальной папки `image/`

---

## ⚠️ Важно:

1. **Session файл обязателен!** Без него бот не будет работать
2. **Структура папок должна быть точной** - `templates/` и `image/` должны быть внутри `/opt/reklama_bot/`
3. **Проверьте после загрузки**, что все файлы на месте

---

## ✅ После загрузки проверьте:

В папке `/opt/reklama_bot/` должно быть:
- [ ] main.py
- [ ] requirements.txt
- [ ] bot_config.json
- [ ] session_name.session
- [ ] templates/ (папка)
  - [ ] index.html (внутри templates/)
- [ ] image/ (папка)
  - [ ] cart_1.png (внутри image/)


