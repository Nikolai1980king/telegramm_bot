# 🔧 Альтернативные способы запуска бота

## Проблема: SSH не работает

Если SSH не подключается, есть другие способы запустить бота.

---

## ✅ Вариант 1: Активировать SSH в панели Beget

1. Войдите в панель Beget: https://cp.beget.com
2. Найдите раздел **"SSH"** или **"Доступ к серверу"**
3. Если есть кнопка **"Активировать SSH"** - нажмите её
4. Создайте/установите пароль SSH
5. Попробуйте подключиться снова

---

## ✅ Вариант 2: Запуск через файловый менеджер Beget

Если в панели Beget есть **"Терминал"** или **"Веб-терминал"**:

1. Откройте файловый менеджер Beget
2. Найдите кнопку **"Терминал"** или **"Консоль"**
3. Откроется веб-терминал прямо в браузере
4. Выполните команды там

---

## ✅ Вариант 3: Настройка через настройки поддомена

1. В панели Beget откройте настройки поддомена `reklama.ятута.рф`
2. Найдите:
   - **"Тип сайта"** → выберите **"Python"** или **"Flask"**
   - **"Путь к приложению"** → `/opt/reklama_bot`
   - **"Файл запуска"** → `main.py`
   - **"Автозапуск"** → включите
3. Сохраните и запустите

---

## ✅ Вариант 4: Создать скрипт автозапуска

Создайте файл `/opt/reklama_bot/start.sh` через файловый менеджер:

```bash
#!/bin/bash
cd /opt/reklama_bot
python3 main.py
```

Установите права:
```bash
chmod +x /opt/reklama_bot/start.sh
```

Затем настройте запуск через cron или через настройки поддомена.

---

## ✅ Вариант 5: Обратиться в поддержку Beget

Напишите в поддержку Beget:

**Текст сообщения:**
```
Здравствуйте! 

Нужна помощь с запуском Flask приложения на поддомене reklama.ятута.рф.

Путь к проекту: /opt/reklama_bot
Файл запуска: main.py

SSH доступ не работает (соединение закрывается).

Как можно запустить Python приложение на этом поддомене?
Нужен ли специальный тариф для Python приложений?

Спасибо!
```

---

## 🎯 Рекомендуемый порядок:

1. **Попробуйте активировать SSH** в панели (Вариант 1)
2. **Проверьте веб-терминал** в файловом менеджере (Вариант 2)
3. **Настройте через поддомен** (Вариант 3)
4. **Обратитесь в поддержку** (Вариант 5)

---

## 📝 Что проверить в панели Beget:

- Раздел **"SSH"** - есть ли кнопка активации?
- Раздел **"Терминал"** или **"Веб-консоль"** в файловом менеджере
- Настройки поддомена `reklama.ятута.рф` - есть ли опции для Python?
- Раздел **"Сайты"** - можно ли создать Python приложение?


