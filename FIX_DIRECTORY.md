# 🔧 Важно: Выполнять команды из правильной директории!

## ❌ Проблема:
Вы были в `/root`, а бот находится в `/opt/reklama_bot`

## ✅ Решение:

```bash
# 1. Перейти в папку бота
cd /opt/reklama_bot

# 2. Проверить процесс
ps aux | grep "python3 main.py" | grep -v grep

# 3. Если процесса нет - запустить
nohup python3 main.py > bot.log 2>&1 &

# 4. Подождать и проверить логи
sleep 5
tail -30 bot.log
```

---

## ⚡ Или всё одной командой:

```bash
cd /opt/reklama_bot && ps aux | grep "python3 main.py" | grep -v grep && echo "---" && nohup python3 main.py > bot.log 2>&1 & sleep 5 && tail -30 bot.log
```

