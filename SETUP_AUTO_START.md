# 🚀 Автозапуск бота (работает независимо от SSH)

## ❌ Проблема:
Когда вы закрываете терминал SSH, бот останавливается.

## ✅ Решение:
Настроить автозапуск через systemd service. Бот будет работать даже после закрытия терминала и автоматически перезапускаться при сбоях.

---

## 📋 Пошаговая инструкция:

### Шаг 1: Подключитесь к серверу

```bash
ssh root@212.67.11.50
```

---

### Шаг 2: Остановите текущий бот (если запущен)

```bash
pkill -f "python3 main.py"
```

---

### Шаг 3: Скопируйте service файл на сервер

**На вашем локальном компьютере** выполните:

```bash
cd ~/PycharmProjects/telegramm_bot
scp reklama_bot.service root@212.67.11.50:/tmp/
```

---

### Шаг 4: На сервере - установите service

**На сервере** выполните:

```bash
# Переместить service файл в нужное место
mv /tmp/reklama_bot.service /etc/systemd/system/

# Перезагрузить systemd
systemctl daemon-reload

# Включить автозапуск
systemctl enable reklama_bot

# Запустить бота
systemctl start reklama_bot

# Проверить статус
systemctl status reklama_bot
```

---

## 🎯 Управление ботом:

### ✅ Проверить статус:
```bash
systemctl status reklama_bot
```

### ▶️ Запустить:
```bash
systemctl start reklama_bot
```

### ⏸️ Остановить:
```bash
systemctl stop reklama_bot
```

### 🔄 Перезапустить:
```bash
systemctl restart reklama_bot
```

### 📋 Посмотреть логи:
```bash
journalctl -u reklama_bot -f
```

Для выхода из логов: **Ctrl+C**

---

## ✅ Преимущества:

- ✅ Бот работает **независимо от SSH**
- ✅ Автоматически **перезапускается при сбоях**
- ✅ Автоматически **запускается при перезагрузке сервера**
- ✅ Удобное **управление через systemctl**
- ✅ **Логи через journalctl**

---

## ⚠️ Важно:

После установки service файла бот будет запускаться автоматически. Если вы ранее запускали бота через `screen`, убедитесь, что старые процессы остановлены:

```bash
pkill -f "python3 main.py"
screen -X -S reklama_bot quit 2>/dev/null || true
```

