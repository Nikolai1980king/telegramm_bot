# 🔧 Бот остановился - диагностика и решение

## Проблема: бот не отвечает, страница недоступна

Возможные причины:
1. Процесс завершился из-за ошибки
2. Сервер перезагрузился
3. Ошибка в коде
4. Проблемы с зависимостями

---

## Диагностика на сервере:

### 1. Проверить, запущен ли процесс:

```bash
ps aux | grep "python3 main.py"
```

Если процесса нет - бот остановился.

### 2. Проверить screen сессии:

```bash
screen -ls
```

### 3. Вернуться к screen сессии (если есть):

```bash
screen -r reklama_bot
```

Посмотрите, что там выводится - могут быть ошибки.

### 4. Проверить порт:

```bash
netstat -tlnp | grep 5001
```

Если порт не слушается - бот не запущен.

---

## Решение: Перезапустить бота

### 1. Остановить все старые процессы:

```bash
pkill -f "python3 main.py"
pkill -f "screen.*reklama_bot"
```

### 2. Запустить заново:

```bash
cd /opt/reklama_bot
screen -S reklama_bot
python3 main.py
```

**Оставьте терминал открытым** - посмотрите, какие ошибки выводятся.

Затем **Ctrl+A**, затем **D** для отключения.

---

## Автоматический перезапуск (рекомендуется):

### Создать systemd service для автозапуска:

Создайте файл `/etc/systemd/system/reklama_bot.service`:

```ini
[Unit]
Description=Telegram Reklama Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/reklama_bot
ExecStart=/usr/bin/python3 /opt/reklama_bot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
systemctl daemon-reload
systemctl enable reklama_bot
systemctl start reklama_bot
systemctl status reklama_bot
```

Бот будет автоматически перезапускаться при сбоях!

---

## Проверка логов:

Если бот падает, посмотрите логи:

```bash
journalctl -u reklama_bot -n 50
```

Или если через screen:

```bash
screen -r reklama_bot
```

---

## Быстрое решение прямо сейчас:

```bash
cd /opt/reklama_bot
pkill -f "python3 main.py"
screen -S reklama_bot
python3 main.py
```

Затем **Ctrl+A**, затем **D**.

Проверьте: `http://212.67.11.50:5001`

