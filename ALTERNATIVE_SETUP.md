# 🔧 Альтернативная настройка без раздела Python

## Вариант 1: Настройка через поддомен (рекомендуется)

1. В панели Beget откройте настройки поддомена `reklama.ятута.рф`
   - Найдите поддомен в списке
   - Нажмите на иконку настроек (шестеренка) или название

2. В настройках поддомена найдите:
   - **"Тип сайта"** или
   - **"Application"** или
   - **"Веб-приложение"**

3. Выберите:
   - **"Python"** или
   - **"Flask"** или
   - **"WSGI"**

4. Укажите:
   - **Путь к приложению**: `/opt/reklama_bot`
   - **Файл запуска**: `main.py`
   - Или **WSGI файл**: `main.py`

5. Сохраните настройки

---

## Вариант 2: Через раздел "Сайты"

1. В панели Beget найдите раздел **"Сайты"** или **"Веб-сайты"**

2. Создайте новый сайт:
   - Нажмите **"Создать сайт"** или **"Добавить"**
   - Выберите поддомен: `reklama.ятута.рф`
   - Выберите тип: **"Python"** или **"Flask"**
   - Укажите путь: `/opt/reklama_bot`

3. Сохраните

---

## Вариант 3: Запуск через SSH (если доступен SSH)

Если у вас есть доступ к SSH, можно запустить бота вручную:

### 3.1. Подключитесь по SSH

```bash
ssh ваш_логин@сервер_beget
```

### 3.2. Установите зависимости

```bash
cd /opt/reklama_bot
pip3 install --user -r requirements.txt
```

### 3.3. Запустите бота в screen/tmux

**Через screen:**
```bash
screen -S reklama_bot
cd /opt/reklama_bot
python3 main.py
# Нажмите Ctrl+A, затем D для отключения
```

**Через tmux:**
```bash
tmux new -s reklama_bot
cd /opt/reklama_bot
python3 main.py
# Нажмите Ctrl+B, затем D для отключения
```

**Вернуться к сессии:**
```bash
screen -r reklama_bot  # для screen
tmux attach -t reklama_bot  # для tmux
```

---

## Вариант 4: Создать WSGI файл (для работы через веб-сервер)

Если Beget поддерживает WSGI, создайте файл `wsgi.py`:

1. В файловом менеджере создайте файл `/opt/reklama_bot/wsgi.py`:

```python
import sys
sys.path.insert(0, '/opt/reklama_bot')

from main import app

if __name__ == "__main__":
    app.run()
```

2. В настройках поддомена укажите:
   - **WSGI файл**: `wsgi.py`
   - **Путь**: `/opt/reklama_bot`

---

## Вариант 5: Через .htaccess (если поддерживается mod_wsgi)

Создайте файл `/opt/reklama_bot/.htaccess`:

```apache
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /opt/reklama_bot/main.py/$1 [L]
```

---

## Вариант 6: Обратиться в поддержку Beget

Если ничего не работает:

1. Напишите в поддержку Beget
2. Спросите: "Как запустить Flask приложение на поддомене?"
3. Укажите:
   - Поддомен: `reklama.ятута.рф`
   - Путь: `/opt/reklama_bot`
   - Файл: `main.py`

---

## 🎯 Рекомендуемый порядок действий:

1. **Сначала попробуйте Вариант 1** - настройки поддомена
2. Если нет - **Вариант 2** - через раздел "Сайты"
3. Если есть SSH - **Вариант 3** - запуск вручную
4. Если ничего не помогает - **Вариант 6** - поддержка Beget

---

## 📝 Что проверить в панели Beget:

- Раздел **"Сайты"** или **"Веб-сайты"**
- Настройки поддомена `reklama.ятута.рф`
- Раздел **"Приложения"** или **"Apps"**
- Раздел **"SSH"** (для доступа к терминалу)


