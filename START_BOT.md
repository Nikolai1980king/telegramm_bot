# 🚀 Как запустить бота на сервере

## Проблема: сайт не открывается

Приложение не запущено. Нужно запустить Flask приложение.

---

## ✅ Способ 1: Через SSH (рекомендуется)

### Шаг 1: Проверьте доступ к SSH

В панели Beget найдите:
- Раздел **"SSH"**
- Или **"Терминал"**
- Или **"Доступ к серверу"**

Если есть - отлично! Если нет - см. Способ 2.

### Шаг 2: Подключитесь по SSH

**Через терминал (Linux/Mac):**
```bash
ssh ваш_логин@212.67.11.50
```

**Через PuTTY (Windows):**
- Хост: `212.67.11.50`
- Порт: `22`
- Логин: ваш логин Beget

### Шаг 3: Установите зависимости

```bash
cd /opt/reklama_bot
pip3 install --user -r requirements.txt
```

### Шаг 4: Запустите бота

**Вариант A: Через screen (рекомендуется):**
```bash
screen -S reklama_bot
cd /opt/reklama_bot
python3 main.py
```

Нажмите **Ctrl+A**, затем **D** для отключения (бот продолжит работать)

**Вернуться к сессии:**
```bash
screen -r reklama_bot
```

**Вариант B: Через nohup:**
```bash
cd /opt/reklama_bot
nohup python3 main.py > bot.log 2>&1 &
```

**Проверить, что запущено:**
```bash
ps aux | grep python
```

**Посмотреть логи:**
```bash
tail -f /opt/reklama_bot/bot.log
```

### Шаг 5: Проверьте работу

Откройте в браузере: `http://reklama.ятута.рф`

---

## ✅ Способ 2: Через панель Beget (если нет SSH)

### Вариант A: Настройки поддомена

1. Откройте настройки поддомена `reklama.ятута.рф`
2. Найдите:
   - **"Тип сайта"** → выберите **"Python"** или **"Flask"**
   - **"Путь к приложению"** → `/opt/reklama_bot`
   - **"Файл запуска"** → `main.py`
3. Сохраните и запустите

### Вариант B: Раздел "Сайты"

1. **Сайты** → **Создать сайт**
2. Выберите поддомен: `reklama.ятута.рф`
3. Тип: **Python/Flask**
4. Путь: `/opt/reklama_bot`
5. Сохраните и запустите

### Вариант C: Обратиться в поддержку

Напишите в поддержку Beget:
- "Как запустить Flask приложение на поддомене reklama.ятута.рф?"
- "Нужен ли специальный тариф для Python приложений?"
- Укажите путь: `/opt/reklama_bot`

---

## 🔍 Проверка работы

После запуска:

1. **Откройте:** `http://reklama.ятута.рф`
2. **Должен открыться веб-интерфейс** управления ботом
3. Если не открывается:
   - Подождите 1-2 минуты
   - Проверьте логи (если запускали через SSH)
   - Проверьте, что процесс запущен

---

## ⚠️ Важно

- Flask приложение должно работать **постоянно**
- Если закрыть терминал без screen/nohup - приложение остановится
- Используйте **screen** или **nohup** для фонового запуска


