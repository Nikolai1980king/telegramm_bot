# 🚀 Запуск бота прямо сейчас

## ✅ Диагностика: бот не запущен

Процесс `/opt/reklama_bot/main.py` не найден. Запускаем!

---

## Шаг 1: Проверить зависимости

```bash
cd /opt/reklama_bot
pip3 list | grep -E "telethon|Flask"
```

Если пакетов нет - установите:

```bash
pip3 install --user --break-system-packages -r requirements.txt
```

---

## Шаг 2: Проверить файлы

```bash
cd /opt/reklama_bot
ls -la
```

Убедитесь, что есть:
- `main.py`
- `session_name.session`
- `bot_config.json`
- `templates/index.html`
- `image/cart_1.png`

---

## Шаг 3: Запустить бота

### Вариант A: Через screen (рекомендуется)

```bash
cd /opt/reklama_bot
screen -S reklama_bot
python3 main.py
```

Затем **Ctrl+A**, затем **D** для отключения.

### Вариант B: Через nohup

```bash
cd /opt/reklama_bot
nohup python3 main.py > bot.log 2>&1 &
```

### Вариант C: Запустить и посмотреть ошибки

```bash
cd /opt/reklama_bot
python3 main.py
```

Оставьте терминал открытым, чтобы видеть ошибки (если будут).

---

## Шаг 4: Проверить, что запустилось

```bash
ps aux | grep "reklama_bot\|/opt/reklama_bot"
```

Должен появиться процесс.

---

## Шаг 5: Проверить веб-интерфейс

Откройте в браузере: `http://reklama.ятута.рф`

---

## Если есть ошибки при запуске:

Пришлите вывод команды `python3 main.py` - помогу исправить!


