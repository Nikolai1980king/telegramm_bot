# 📤 Пошаговая загрузка обновленного main.py на сервер

## ✅ Что изменилось:

Добавлена автоматическая перезагрузка конфига - теперь изменения применяются без перезапуска бота!

---

## 📋 Шаг 1: Загрузить файл на сервер

### Способ 1: Через scp (рекомендуется)

**На вашем локальном компьютере** выполните:

```bash
cd ~/PycharmProjects/telegramm_bot
scp main.py root@212.67.11.50:/opt/reklama_bot/
```

Введите пароль от сервера, когда попросит.

---

### Способ 2: Через файловый менеджер (если есть доступ)

1. Откройте файловый менеджер (FileZilla, WinSCP, или встроенный в панель Beget)
2. Подключитесь к серверу
3. Найдите файл `main.py` в папке `~/PycharmProjects/telegramm_bot/`
4. Загрузите его в `/opt/reklama_bot/` на сервере (замените старый)

---

## 📋 Шаг 2: На сервере - перезапустить бот

После загрузки файла **на сервере** выполните:

```bash
cd /opt/reklama_bot

# 1. Создать резервную копию (на всякий случай)
cp main.py main.py.backup_$(date +%Y%m%d_%H%M%S)

# 2. Проверить синтаксис нового файла
python3 -m py_compile main.py && echo "✅ Синтаксис правильный!" || echo "❌ Ошибка синтаксиса!"

# 3. Остановить бот
pkill -9 -f "python3 main.py"

# 4. Подождать
sleep 2

# 5. Запустить бот с новым кодом
nohup python3 main.py > bot.log 2>&1 &

# 6. Проверить логи
sleep 5
tail -30 bot.log
```

---

## ⚡ Или всё одной командой на сервере:

```bash
cd /opt/reklama_bot && cp main.py main.py.backup_$(date +%Y%m%d_%H%M%S) && python3 -m py_compile main.py && pkill -9 -f "python3 main.py" && sleep 2 && nohup python3 main.py > bot.log 2>&1 & sleep 5 && tail -30 bot.log
```

---

## ✅ Проверка:

После перезапуска:

1. **Проверьте логи** - не должно быть ошибок
2. **Проверьте веб-интерфейс**: http://212.67.11.50:5001
3. **Измените настройки** - теперь они применятся автоматически!

---

## 🎯 Что теперь работает:

- ✅ Изменения интервала применяются автоматически
- ✅ Изменения расписания применяются автоматически  
- ✅ Изменения получателей применяются автоматически
- ✅ Не нужно перезапускать бота вручную

