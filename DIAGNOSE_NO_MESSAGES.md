# 🔍 Диагностика: Бот не отправляет сообщения

## ⚡ Быстрая проверка на сервере:

Подключитесь к серверу и выполните эти команды:

```bash
ssh root@212.67.11.50
cd /opt/reklama_bot
```

### 1. Проверить, запущен ли бот:

```bash
ps aux | grep "python3 main.py"
```

Если процесса нет - бот не запущен. Запустите:
```bash
nohup python3 main.py > bot.log 2>&1 &
```

### 2. Проверить логи:

```bash
tail -100 bot.log
```

Ищите ошибки или сообщения о расписании.

### 3. Проверить конфигурацию:

```bash
cat bot_config.json | python3 -m json.tool
```

Проверьте:
- `is_running: true`
- `schedule_type` - должен быть "interval" или "schedule"
- Если `schedule_type: "schedule"` - проверьте `schedule_hours` и `schedule_days`

### 4. Проверить время сервера:

```bash
date
```

Бот работает по времени сервера!

---

## 🔧 Частые проблемы:

### Проблема 1: Бот не запущен
**Решение:** Запустите бота (см. выше)

### Проблема 2: Используется расписание, но не подходящее время
**Симптомы:** В логах видно "⏰ Следующая отправка по расписанию..."
**Решение:** 
- Проверьте текущее время и настройки расписания
- Измените расписание через веб-интерфейс
- Или переключитесь на режим интервала

### Проблема 3: Интервал еще не прошел
**Симптомы:** Последняя отправка была недавно, следующий интервал еще не наступил
**Решение:** Подождите или уменьшите интервал временно

### Проблема 4: Ошибка авторизации
**Симптомы:** В логах "❌ Требуется авторизация!"
**Решение:** 
- Проверьте наличие session файла
- Если его нет - запустите бота локально для авторизации

### Проблема 5: Файл изображения не найден
**Симптомы:** В логах "⚠️ Файл не найден"
**Решение:** Проверьте путь к файлу в конфиге

---

## 🛠️ Используйте скрипт диагностики:

Скопируйте файл `check_bot.py` на сервер и запустите:

```bash
# На локальной машине
scp check_bot.py root@212.67.11.50:/opt/reklama_bot/

# На сервере
cd /opt/reklama_bot
python3 check_bot.py
```

---

## 📋 Что прислать для диагностики:

Выполните эти команды и пришлите вывод:

```bash
cd /opt/reklama_bot
echo "=== ПРОЦЕСС ===" && ps aux | grep "python3 main.py"
echo -e "\n=== ЛОГИ (последние 50 строк) ===" && tail -50 bot.log
echo -e "\n=== КОНФИГ ===" && cat bot_config.json
echo -e "\n=== ВРЕМЯ ===" && date
```

