# 🔍 Где найти настройки прокси в панели Beget

## ✅ DNS настроен правильно!

A-запись для `reklama.ятута.рф` указывает на `5.101.152.21` - это правильно.

Теперь нужно настроить **веб-сервер**, чтобы он проксировал запросы на порт 5001.

---

## Где искать настройки прокси:

### 1. Раздел "Сайты" или "Веб-сайты"

В главном меню панели Beget найдите:
- **"Сайты"**
- **"Веб-сайты"**
- **"Управление сайтами"**

Там должны быть настройки для каждого поддомена.

### 2. Настройки поддомена напрямую

В разделе **"Домены"** → **"Поддомены"**:
- Найдите `reklama.ятута.рф`
- Нажмите на иконку **шестеренки** (настройки) или на название поддомена
- Должны открыться настройки сайта

### 3. Раздел "Apache" или "Nginx"

Если есть разделы:
- **"Apache"**
- **"Nginx"**
- **"Веб-сервер"**

Там можно настроить виртуальные хосты.

### 4. Файловый менеджер + .htaccess

Если есть доступ к файловому менеджеру:
- Откройте папку, куда направлен поддомен (возможно `/opt/reklama_bot` или другая)
- Создайте файл `.htaccess` с настройками прокси

---

## Что искать в настройках:

- **"Прокси"** или **"Reverse Proxy"**
- **"Настройки сайта"**
- **"Тип сайта"** → выбрать "Прокси" или "Reverse Proxy"
- **"URL проксирования"** → указать `http://127.0.0.1:5001`
- **"Порт приложения"** → указать `5001`

---

## Альтернатива: Создать .htaccess файл

Если поддомен направлен на папку `/opt/reklama_bot`, создайте файл `.htaccess`:

```apache
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ http://127.0.0.1:5001/$1 [P,L]
```

Но для этого нужен модуль mod_proxy в Apache.

---

## Рекомендация:

1. **Проверьте раздел "Сайты"** в главном меню панели
2. **Или настройки поддомена** (иконка шестеренки рядом с `reklama.ятута.рф`)
3. **Или обратитесь в поддержку Beget** - они подскажут, где настроить прокси

---

## Временное решение:

Пока настраиваете прокси, можно использовать прямой доступ:
- `http://reklama.ятута.рф:5001`
- `http://212.67.11.50:5001`


