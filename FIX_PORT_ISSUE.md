# 🔧 Исправление проблемы с портом

## Проблема: порт 5000 занят

Нужно найти и остановить процесс, занимающий порт 5000, или использовать порт 5001.

---

## Шаг 1: Найти процесс на порту 5000

```bash
lsof -i :5000
```

или

```bash
netstat -tlnp | grep :5000
```

или

```bash
ss -tlnp | grep :5000
```

---

## Шаг 2: Остановить процесс

Если это старый бот:

```bash
# Найти PID процесса
lsof -i :5000 | grep python

# Остановить по PID (замените PID на реальный)
kill -9 PID

# Или остановить все python процессы на порту 5000
fuser -k 5000/tcp
```

---

## Шаг 3: Проверить порт в main.py

```bash
cd /opt/reklama_bot
grep "port=" main.py
```

Должно быть `port=5001`, если нет - измените:

```bash
sed -i "s/port=5000/port=5001/g" main.py
```

---

## Шаг 4: Загрузить обновленные файлы

Через файловый менеджер или SCP загрузите:
- `main.py`
- `templates/index.html`

---

## Шаг 5: Запустить бота

```bash
cd /opt/reklama_bot
screen -S reklama_bot
python3 main.py
```

Затем **Ctrl+A**, затем **D**.


