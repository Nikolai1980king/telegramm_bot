# 🔄 Перезапуск бота после обновления

## После загрузки файлов на сервер:

Выполните на сервере:

```bash
cd /opt/reklama_bot

# 1. Создать резервную копию (на всякий случай)
cp main.py main.py.backup_$(date +%Y%m%d_%H%M%S)
cp templates/index.html templates/index.html.backup_$(date +%Y%m%d_%H%M%S)

# 2. Проверить синтаксис
python3 -m py_compile main.py && echo "✅ Синтаксис правильный!" || echo "❌ Ошибка синтаксиса!"

# 3. Остановить бот
pkill -9 -f "python3 main.py"

# 4. Подождать
sleep 2

# 5. Перезапустить бот
nohup python3 main.py > bot.log 2>&1 &

# 6. Проверить логи
sleep 5
tail -30 bot.log
```

---

## ⚡ Или всё одной командой:

```bash
cd /opt/reklama_bot && python3 -m py_compile main.py && pkill -9 -f "python3 main.py" && sleep 2 && nohup python3 main.py > bot.log 2>&1 & sleep 5 && tail -30 bot.log
```

---

## ✅ Проверка:

1. **Проверить процесс:**
   ```bash
   ps aux | grep "python3 main.py" | grep -v grep
   ```

2. **Проверить веб-интерфейс:**
   Откройте: http://212.67.11.50:5001
   - Должна появиться секция "📊 Статистика отправки сообщений"
   - Статистика будет пустой до первой отправки

3. **Проверить логи:**
   ```bash
   tail -50 bot.log
   ```

---

## 🎯 После перезапуска:

1. Запустите бота через веб-интерфейс
2. Дождитесь первой отправки
3. Проверьте статистику в веб-интерфейсе

