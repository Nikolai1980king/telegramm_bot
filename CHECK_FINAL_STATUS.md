# ✅ Проверка финального статуса

## Команды для проверки:

```bash
cd /opt/reklama_bot

# 1. Проверить процесс
ps aux | grep "python3 main.py" | grep -v grep

# 2. Проверить порт 5001
netstat -tlnp | grep 5001

# 3. Посмотреть все логи
tail -50 bot.log

# 4. Проверить авторизацию и отправку
grep -E "Авторизация|авторизация|Отправлено|отправка|error|Error|ERROR" bot.log | tail -20
```

---

## ✅ Что должно быть:

1. **Процесс запущен** - `ps aux | grep "python3 main.py"` показывает процесс
2. **Порт 5001 открыт** - `netstat -tlnp | grep 5001` показывает прослушивание
3. **Авторизация успешна** - в логах "✅ Авторизация успешна!"
4. **Отправка сообщений** - в логах "⏳ Следующая отправка через..." или "🖼️ Отправлено"

---

## 🌐 Проверка веб-интерфейса:

Откройте: http://212.67.11.50:5001

Должен открыться веб-интерфейс управления ботом.

