# 🔌 Варианты подключения по SSH

## Вариант 1: Как root по IP

```bash
ssh root@212.67.11.50
```

**Важно:** На Beget обычно не используют root, но можно попробовать.

---

## Вариант 2: С вашим логином по IP

```bash
ssh kapita6n@212.67.11.50
```

---

## Вариант 3: С вашим логином по домену

```bash
ssh kapita6n@kapita6n.beget.tech
```

---

## Вариант 4: С указанием порта (если не стандартный)

```bash
ssh -p 22 kapita6n@212.67.11.50
```

или

```bash
ssh -p 22 root@212.67.11.50
```

---

## После подключения выполните:

```bash
# Перейти в папку проекта
cd /opt/reklama_bot

# Проверить файлы
ls -la

# Установить зависимости
pip3 install --user -r requirements.txt

# Запустить бота
screen -S reklama_bot
python3 main.py
```

Затем **Ctrl+A**, затем **D** для отключения.

---

## Если ни один вариант не работает:

1. Проверьте пароль SSH в панели Beget
2. Убедитесь, что SSH доступ активирован
3. Попробуйте веб-терминал в файловом менеджере Beget
4. Обратитесь в поддержку Beget


