# 🔧 Исправление проблемы со screen

## Проблема: Много screen сессий

Создано несколько сессий. Нужно почистить и запустить заново.

---

## Шаг 1: Убить все старые сессии

```bash
screen -X -S 1855213.reklama_bot quit
screen -X -S 1854955.reklama_bot quit
screen -X -S 1854944.reklama_bot quit
screen -X -S 1854930.reklama_bot quit
screen -X -S 1854915.reklama_bot quit
```

Или проще - убить все сессии с именем reklama_bot:

```bash
screen -S reklama_bot -X quit
```

Повторите несколько раз, пока не убьете все.

---

## Шаг 2: Проверить, что все сессии закрыты

```bash
screen -ls
```

Должно быть пусто или только другие сессии.

---

## Шаг 3: Запустить бота заново

```bash
cd /opt/reklama_bot
screen -S reklama_bot
python3 main.py
```

**Оставьте терминал открытым** - вы увидите вывод!

---

## Шаг 4: Если нужно посмотреть, что было в старой сессии

```bash
screen -r 1855213.reklama_bot
```

Посмотрите, что там выводится - возможно, там есть ошибки.

---

## Альтернатива: Запустить БЕЗ screen (чтобы увидеть ошибки)

```bash
cd /opt/reklama_bot
python3 main.py
```

Оставьте терминал открытым - увидите все ошибки сразу.


