# 🚀 Команды для выполнения на сервере

Вы уже подключены к серверу! Выполните эти команды:

```bash
# 1. Перейти в папку бота
cd /opt/reklama_bot

# 2. Остановить старый бот (если запущен)
pkill -f "python3 main.py"

# 3. Запустить бота через nohup
nohup python3 main.py > bot.log 2>&1 &

# 4. Проверить, что бот запущен
ps aux | grep "python3 main.py"

# 5. Посмотреть последние строки лога
tail -20 bot.log
```

После выполнения команды `nohup` вы увидите что-то вроде:
```
[1] 1234567
```

Это означает, что процесс запущен с PID 1234567.

---

## ✅ Проверка работы:

1. **Проверьте процесс:**
   ```bash
   ps aux | grep "python3 main.py"
   ```

2. **Проверьте порт:**
   ```bash
   netstat -tlnp | grep 5001
   ```

3. **Посмотрите логи:**
   ```bash
   tail -f /opt/reklama_bot/bot.log
   ```
   (Выход: Ctrl+C)

---

## 🌐 Проверка веб-интерфейса:

Откройте в браузере:
- http://212.67.11.50:5001
- http://reklama.ятута.рф:5001 (если настроен reverse proxy)

---

## ⚠️ Важно:

После запуска через `nohup`:
- ✅ Бот продолжит работать после закрытия терминала
- ✅ Все логи сохраняются в файл `bot.log`
- ❌ Бот НЕ перезапустится автоматически при сбое
- ❌ Бот НЕ запустится автоматически при перезагрузке сервера

Для автоматического перезапуска используйте systemd service (см. `SETUP_AUTO_START.md`).

