# 🔧 Настройка бота через SSH

## Шаг 1: Получить данные для SSH подключения

В панели Beget найдите раздел **"SSH"** или **"Доступ к серверу"**:

1. **Логин SSH** (обычно это ваш логин Beget)
2. **Пароль SSH** (может отличаться от пароля панели)
3. **Хост/Сервер** (обычно `ваш_логин.beget.tech` или IP адрес)
4. **Порт** (обычно `22`)

---

## Шаг 2: Подключиться к серверу

### На Linux/Mac:

Откройте терминал и выполните:
```bash
ssh ваш_логин@ваш_сервер.beget.tech
```

Или если есть IP:
```bash
ssh ваш_логин@212.67.11.50
```

Введите пароль SSH (при запросе).

### На Windows:

**Вариант A: Через PuTTY**
1. Скачайте PuTTY: https://www.putty.org/
2. Запустите PuTTY
3. Заполните:
   - Host Name: `ваш_логин.beget.tech` или IP
   - Port: `22`
   - Connection type: SSH
4. Нажмите "Open"
5. Введите логин и пароль

**Вариант B: Через Windows Terminal (Windows 10/11)**
1. Откройте Windows Terminal
2. Выполните:
   ```bash
   ssh ваш_логин@ваш_сервер.beget.tech
   ```

---

## Шаг 3: После подключения

Когда подключитесь, вы увидите приглашение командной строки, например:
```
[ваш_логин@сервер ~]$
```

---

## Шаг 4: Перейти в папку проекта

```bash
cd /opt/reklama_bot
```

Проверьте, что файлы на месте:
```bash
ls -la
```

Должны быть видны:
- main.py
- requirements.txt
- bot_config.json
- session_name.session
- templates/
- image/

---

## Шаг 5: Установить зависимости

```bash
pip3 install --user -r requirements.txt
```

Если pip3 не найден, попробуйте:
```bash
python3 -m pip install --user -r requirements.txt
```

Или установите вручную:
```bash
pip3 install --user telethon Flask cryptg
```

---

## Шаг 6: Проверить Python

```bash
python3 --version
```

Должна быть версия 3.6 или выше.

---

## Шаг 7: Запустить бота

### Вариант A: Через screen (рекомендуется)

```bash
# Установить screen (если нет)
# Обычно уже установлен

# Создать новую screen сессию
screen -S reklama_bot

# Перейти в папку проекта
cd /opt/reklama_bot

# Запустить бота
python3 main.py
```

**Бот запустится и начнет работать.**

**Чтобы отключиться (бот продолжит работать):**
- Нажмите: **Ctrl+A**, затем **D**

**Чтобы вернуться к сессии:**
```bash
screen -r reklama_bot
```

**Чтобы остановить бота:**
```bash
screen -r reklama_bot
# Нажмите Ctrl+C
```

### Вариант B: Через nohup

```bash
cd /opt/reklama_bot
nohup python3 main.py > bot.log 2>&1 &
```

**Проверить, что запущено:**
```bash
ps aux | grep python
```

**Посмотреть логи:**
```bash
tail -f /opt/reklama_bot/bot.log
```

**Остановить бота:**
```bash
pkill -f "python3 main.py"
```

---

## Шаг 8: Проверить работу

1. Откройте в браузере: `http://reklama.ятута.рф`
2. Должен открыться веб-интерфейс управления ботом
3. Если не открывается - проверьте логи

---

## 🔍 Полезные команды

**Проверить, запущен ли бот:**
```bash
ps aux | grep python
```

**Посмотреть логи (если через nohup):**
```bash
tail -f /opt/reklama_bot/bot.log
```

**Посмотреть логи (если через screen):**
```bash
screen -r reklama_bot
```

**Остановить бота:**
```bash
# Если через screen
screen -r reklama_bot
# Затем Ctrl+C

# Если через nohup
pkill -f "python3 main.py"
```

**Перезапустить бота:**
```bash
# Остановить
pkill -f "python3 main.py"

# Запустить снова
cd /opt/reklama_bot
screen -S reklama_bot
python3 main.py
# Ctrl+A, затем D
```

---

## ⚠️ Важно

- Бот должен работать **постоянно**
- Используйте **screen** для фонового запуска
- Не закрывайте SSH соединение без screen/nohup - бот остановится
- Если бот остановился - запустите снова через screen

---

## 🎯 Быстрая команда для запуска:

```bash
cd /opt/reklama_bot && screen -S reklama_bot python3 main.py
```

Затем нажмите **Ctrl+A**, затем **D**


