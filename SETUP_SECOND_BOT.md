# Настройка второго бота на поддомене

## Важно: Изолированные экземпляры

Каждый бот должен работать **независимо**:
- ✅ Своя папка
- ✅ Свой конфиг (`bot_config.json`)
- ✅ Свой session файл
- ✅ Свой поддомен

---

## Шаг 1: Создание отдельной директории для второго бота

На вашем сервере создайте новую папку для второго бота:

```
/home/u1234567/
├── telegram_bot_1/          # Первый бот (уже существует)
│   ├── main.py
│   ├── bot_config.json
│   └── session_name.session
│
└── telegram_bot_2/          # Второй бот (новый)
    ├── main.py
    ├── bot_config.json
    ├── session_name_2.session
    └── image/
        └── cart_1.png
```

---

## Шаг 2: Копирование файлов

### Вариант A: Через FTP/SFTP

1. Скачайте все файлы первого бота на компьютер
2. Создайте новую папку `telegram_bot_2` на сервере
3. Загрузите файлы во вторую папку

### Вариант B: Через SSH (если доступен)

```bash
cd /home/u1234567/
cp -r telegram_bot_1 telegram_bot_2
cd telegram_bot_2
```

---

## Шаг 3: Настройка второго бота

### 3.1. Обновите `bot_config.json` второго бота:

```json
{
    "api_id": "27375139",
    "api_hash": "66e1bc627b8dda02e2bb35ea44fde4cf",
    "phone_number": "+79140024032",
    "recipients": ["@recipient1", "@recipient2"],
    "interval": 32400,
    "photo_path": "image/cart_1.png",
    "caption": "здесь будет видеоролик -переходи в приложение @znakomstva_v_kafe_bot",
    "is_running": false,
    "session_file": "session_name_2"  // ← ВАЖНО: другое имя!
}
```

**Ключевые изменения:**
- ✅ `session_file`: `"session_name_2"` (чтобы не конфликтовал с первым ботом)
- ✅ Можно использовать **тот же номер телефона** (один аккаунт, разные session)
- ✅ Или другой аккаунт (другие `api_id`, `api_hash`, `phone_number`)

### 3.2. Если используете тот же аккаунт:

Session файлы будут разные, но авторизация одна. Просто используйте разные имена session файлов:
- Первый бот: `session_name.session`
- Второй бот: `session_name_2.session`

### 3.3. Если используете другой аккаунт:

1. Обновите `api_id`, `api_hash`, `phone_number` в конфиге
2. При первом запуске создадите новый session файл

---

## Шаг 4: Настройка поддомена в Beget

### 4.1. Создайте поддомен:

1. Войдите в панель управления Beget
2. Перейдите в раздел "Домены" → "Поддомены"
3. Создайте новый поддомен (например: `bot2.yourdomain.com`)
4. Укажите путь к папке второго бота: `/home/u1234567/telegram_bot_2`

### 4.2. Настройте Python приложение:

1. В панели Beget найдите раздел "Python" или "Python приложения"
2. Создайте новое Python приложение
3. Укажите:
   - **Путь к проекту**: `/home/u1234567/telegram_bot_2`
   - **Файл запуска**: `main.py`
   - **Поддомен/домен**: `bot2.yourdomain.com` (созданный поддомен)
   - **Порт**: (обычно настраивается автоматически)

4. Сохраните настройки

---

## Шаг 5: Первая авторизация второго бота

### 5.1. Локально (рекомендуется):

1. Запустите второй бот локально один раз:
   ```bash
   cd telegram_bot_2
   python main.py
   ```

2. Введите код авторизации из Telegram

3. После авторизации появятся файлы:
   - `session_name_2.session`
   - `session_name_2.session-journal`

4. Загрузите эти файлы на сервер в папку `telegram_bot_2/`

### 5.2. Или на сервере (если есть доступ к терминалу):

```bash
cd /home/u1234567/telegram_bot_2
python3 main.py
```

---

## Шаг 6: Проверка работы

1. Откройте веб-интерфейс второго бота: `http://bot2.yourdomain.com`
2. Проверьте настройки
3. Запустите бота через интерфейс
4. Проверьте, что оба бота работают независимо

---

## Важные моменты

### ✅ Можно использовать один аккаунт для обоих ботов:
- Разные session файлы (не конфликтуют)
- Разные получатели
- Разные настройки

### ✅ Или разные аккаунты:
- Полная изоляция
- Больше возможностей

### ⚠️ Ограничения Telegram:
- Соблюдайте лимиты на отправку сообщений
- Не спамьте - рискуете получить блокировку

### 💡 Рекомендации:
- Разные интервалы отправки для каждого бота
- Разные получатели
- Мониторьте логи обоих ботов

---

## Структура на сервере (пример)

```
/home/u1234567/
├── telegram_bot_1/              # Первый бот
│   ├── main.py
│   ├── bot_config.json          # Конфиг для бота 1
│   ├── session_name.session     # Session бота 1
│   ├── templates/
│   └── image/
│
└── telegram_bot_2/              # Второй бот (реклама)
    ├── main.py
    ├── bot_config.json          # Конфиг для бота 2
    ├── session_name_2.session   # Session бота 2
    ├── templates/
    └── image/
```

---

## Решение проблем

### Оба бота конфликтуют:
- Убедитесь, что используются разные session файлы
- Проверьте, что конфиги в разных папках

### Второй бот не запускается:
- Проверьте пути в `bot_config.json`
- Убедитесь, что session файл загружен
- Проверьте логи в панели Beget

### Поддомен не работает:
- Проверьте настройки поддомена в панели
- Убедитесь, что Python приложение привязано к поддомену
- Проверьте DNS настройки (обычно автоматически)


