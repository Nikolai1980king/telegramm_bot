# 🔧 Исправление синтаксической ошибки

## Проблема:
```
SyntaxError: name 'bot_thread' is assigned to before global declaration
```

## ✅ Быстрое исправление на сервере:

```bash
cd /opt/reklama_bot

# Создать резервную копию
cp main.py main.py.backup

# Удалить строку с "global bot_thread" (строка 553)
sed -i '553d' main.py

# Проверить синтаксис
python3 -m py_compile main.py && echo "✅ OK!" || echo "❌ Ошибка!"

# Перезапустить бот
pkill -9 -f "python3 main.py"
sleep 2
nohup python3 main.py > bot.log 2>&1 &

# Проверить логи
sleep 5
tail -30 bot.log
```

---

## ✅ Альтернатива: Редактировать через nano

```bash
cd /opt/reklama_bot
nano main.py
```

Найдите строку 553 (Ctrl+_ и введите 553):
- Найдите: `global bot_thread`
- Удалите эту строку (Ctrl+K)
- Сохраните: Ctrl+O, Enter, Ctrl+X

---

## Проверка:

После исправления в логах НЕ должно быть SyntaxError.

