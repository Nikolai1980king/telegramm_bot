# 🔄 Перезапуск бота на сервере

## Важно: Выполняйте команды НА СЕРВЕРЕ!

Сейчас вы на локальном компьютере. Нужно подключиться к серверу.

---

## Шаг 1: Подключиться к серверу

```bash
ssh root@212.67.11.50
```

Введите пароль.

---

## Шаг 2: Проверить бота на сервере

После подключения к серверу выполните:

```bash
ps aux | grep "python3 main.py"
```

Если процесса нет - бот остановился.

---

## Шаг 3: Перезапустить бота

```bash
cd /opt/reklama_bot
pkill -f "python3 main.py"
screen -S reklama_bot
python3 main.py
```

**Оставьте терминал открытым** - посмотрите ошибки (если есть).

Затем **Ctrl+A**, затем **D** (бот продолжит работать).

---

## Шаг 4: Проверить работу

```bash
ps aux | grep "python3 main.py"
netstat -tlnp | grep 5001
```

Должен быть процесс и порт должен слушаться.

---

## Если есть ошибки:

Вернитесь к screen сессии:

```bash
screen -r reklama_bot
```

Пришлите вывод - помогу исправить.

