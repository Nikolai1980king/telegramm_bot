# ✅ Проверка статуса бота

## Выполните на сервере:

```bash
cd /opt/reklama_bot

# 1. Проверить процесс
ps aux | grep "python3 main.py" | grep -v grep

# 2. Посмотреть ВСЕ логи (последние 100 строк)
tail -100 bot.log

# 3. Проверить автозапуск
grep -E "Автозапуск|авторизация|Авторизация|error|ERROR|❌" bot.log | tail -20

# 4. Проверить конфиг
cat bot_config.json | grep is_running

# 5. Проверить порт
netstat -tlnp | grep 5001
```

---

## Что должно быть:

- ✅ Flask запущен на порту 5001
- ✅ Telegram бот запущен (если is_running = true)
- ✅ Сообщение "🚀 Автозапуск бота" в логах (если бот должен быть запущен)
- ✅ Сообщение "✅ Авторизация успешна!" в логах

---

## Если бот не запущен:

1. Откройте веб-интерфейс: http://212.67.11.50:5001
2. Нажмите "Запустить бота"
3. Проверьте логи снова

