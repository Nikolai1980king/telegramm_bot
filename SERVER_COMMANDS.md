# 🚀 Команды для выполнения на сервере

## ✅ Вы подключены! Теперь выполните:

### 1. Установить зависимости (с обходом ограничения):

```bash
pip3 install --user --break-system-packages -r requirements.txt
```

Или создать виртуальное окружение (рекомендуется):

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Проверить Python:

```bash
python3 --version
```

### 3. Запустить бота:

**Вариант A: Через screen (рекомендуется):**

```bash
screen -S reklama_bot
python3 main.py
```

Затем **Ctrl+A**, затем **D** для отключения.

**Вариант B: Если используете venv:**

```bash
screen -S reklama_bot
source venv/bin/activate
python main.py
```

Затем **Ctrl+A**, затем **D**.

**Вариант C: Через nohup:**

```bash
nohup python3 main.py > bot.log 2>&1 &
```

### 4. Проверить работу:

Откройте в браузере: `http://reklama.ятута.рф`

---

## Полезные команды:

**Вернуться к сессии screen:**
```bash
screen -r reklama_bot
```

**Проверить, запущен ли бот:**
```bash
ps aux | grep python
```

**Посмотреть логи (если через nohup):**
```bash
tail -f /opt/reklama_bot/bot.log
```

**Остановить бота:**
```bash
pkill -f "python3 main.py"
```


