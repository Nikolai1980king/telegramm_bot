# 🚀 Команды для настройки бота через SSH

## Данные для подключения:
- **Сервер:** `kapita6n.beget.tech`
- **Логин:** `kapita6n` (скорее всего)
- **Порт:** `22` (по умолчанию)

---

## Шаг 1: Подключиться к серверу

### На Linux/Mac:
```bash
ssh kapita6n@kapita6n.beget.tech
```

### На Windows (PuTTY или Terminal):
```bash
ssh kapita6n@kapita6n.beget.tech
```

Введите пароль SSH (при запросе).

---

## Шаг 2: После подключения выполните команды:

```bash
# 1. Перейти в папку проекта
cd /opt/reklama_bot

# 2. Проверить, что файлы на месте
ls -la

# 3. Установить зависимости
pip3 install --user -r requirements.txt

# 4. Проверить версию Python
python3 --version
```

---

## Шаг 3: Запустить бота

### Вариант A: Через screen (рекомендуется)

```bash
# Создать screen сессию
screen -S reklama_bot

# Запустить бота
cd /opt/reklama_bot
python3 main.py
```

**Чтобы отключиться (бот продолжит работать):**
- Нажмите: **Ctrl+A**, затем **D**

**Чтобы вернуться к сессии:**
```bash
screen -r reklama_bot
```

**Чтобы остановить бота:**
```bash
screen -r reklama_bot
# Нажмите Ctrl+C
```

### Вариант B: Через nohup

```bash
cd /opt/reklama_bot
nohup python3 main.py > bot.log 2>&1 &
```

**Проверить, что запущено:**
```bash
ps aux | grep python
```

**Посмотреть логи:**
```bash
tail -f /opt/reklama_bot/bot.log
```

---

## Шаг 4: Проверить работу

Откройте в браузере: `http://reklama.ятута.рф`

Должен открыться веб-интерфейс управления ботом!

---

## Полезные команды:

**Проверить, запущен ли бот:**
```bash
ps aux | grep python
```

**Посмотреть логи:**
```bash
# Если через screen
screen -r reklama_bot

# Если через nohup
tail -f /opt/reklama_bot/bot.log
```

**Остановить бота:**
```bash
pkill -f "python3 main.py"
```

**Перезапустить бота:**
```bash
pkill -f "python3 main.py"
cd /opt/reklama_bot
screen -S reklama_bot
python3 main.py
# Ctrl+A, затем D
```


