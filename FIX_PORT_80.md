# 🔧 Порт 80 занят - решение

## Проблема: порт 80 занят другим процессом

Порт 80 обычно занят веб-сервером (Apache/Nginx). Лучше вернуться к порту 5001 и настроить реверс-прокси.

---

## Решение: Вернуться к порту 5001 и настроить прокси

### 1. Вернуть порт 5001:

```bash
cd /opt/reklama_bot

# Остановить бота
pkill -f "python3 main.py"

# Вернуть порт 5001
sed -i "s/port=80/port=5001/g" main.py

# Запустить заново
screen -S reklama_bot
python3 main.py
# Ctrl+A, затем D
```

### 2. Настроить реверс-прокси в панели Beget:

1. Войдите в панель Beget
2. Откройте настройки поддомена `reklama.ятута.рф`
3. Найдите настройки прокси или реверс-прокси
4. Укажите: `http://127.0.0.1:5001`
5. Сохраните

---

## Альтернатива: Проверить, что занимает порт 80

Если хотите использовать порт 80:

```bash
# Найти процесс, занимающий порт 80
lsof -i :80
# или
netstat -tlnp | grep :80
```

Обычно это Apache или Nginx. Можно остановить, но это может повлиять на другие сайты.

---

## Рекомендация:

**Используйте порт 5001 и настройте реверс-прокси через панель Beget** - это безопаснее и не повлияет на другие сайты.


