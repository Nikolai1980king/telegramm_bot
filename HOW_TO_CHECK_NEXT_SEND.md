# 📅 Как узнать время следующей отправки

## Способ 1: Посмотреть в логах бота (проще всего)

На сервере выполните:

```bash
cd /opt/reklama_bot

# Посмотреть последние логи
tail -50 bot.log | grep -E "Следующая отправка|следующая|Отправлено"
```

Вы увидите строки типа:
- `⏳ Следующая отправка через 2 часов...` (режим интервала)
- `⏰ Следующая отправка по расписанию: 14:00 04.12.2025` (режим расписания)
- `🖼️ Отправлено для @username в 10:00 04.12.2025` (время последней отправки)

---

## Способ 2: Использовать скрипт проверки

### Шаг 1: Загрузить скрипт на сервер

**На вашем компьютере:**
```bash
cd ~/PycharmProjects/telegramm_bot
scp check_next_send.py root@212.67.11.50:/opt/reklama_bot/
```

### Шаг 2: Запустить скрипт на сервере

```bash
cd /opt/reklama_bot
python3 check_next_send.py
```

Скрипт покажет:
- Текущее время
- Режим работы (интервал/расписание)
- Точное время следующей отправки
- Сколько осталось до следующей отправки

---

## Способ 3: Посмотреть в веб-интерфейсе

Откройте: http://212.67.11.50:5001

В интерфейсе вы увидите:
- Текущий статус бота
- Настройки интервала/расписания
- Можете вычислить время следующей отправки вручную

---

## Способ 4: Проверить вручную

### Для режима интервала:

1. Посмотрите время последней отправки в логах:
   ```bash
   grep "Отправлено" /opt/reklama_bot/bot.log | tail -1
   ```

2. Посмотрите интервал в конфиге:
   ```bash
   cat /opt/reklama_bot/bot_config.json | grep interval
   ```

3. Вычислите: Время последней отправки + Интервал = Следующая отправка

### Для режима расписания:

1. Посмотрите настройки в конфиге:
   ```bash
   cat /opt/reklama_bot/bot_config.json | grep -E "schedule_hours|schedule_days"
   ```

2. Определите следующий час из списка часов
3. Учтите дни недели и четность даты

---

## 📋 Примеры команд:

```bash
cd /opt/reklama_bot

# Все способы сразу
echo "=== В ЛОГАХ ===" && tail -50 bot.log | grep -E "Следующая отправка|Отправлено" && echo -e "\n=== КОНФИГ ===" && cat bot_config.json | grep -E "interval|schedule" && echo -e "\n=== ВРЕМЯ СЕРВЕРА ===" && date
```

---

## 💡 Рекомендация:

**Самый простой способ** - посмотреть в логах:

```bash
tail -30 /opt/reklama_bot/bot.log | grep "Следующая отправка"
```

Или использовать скрипт `check_next_send.py` - он покажет всю информацию сразу.

