# 🌐 Настройка поддомена reklama.ятута.рф

## Проблема: поддомен не работает

Поддомен создан, но не настроен на порт 5001, где работает бот.

---

## Решение 1: Настроить реверс-прокси в панели Beget

1. Войдите в панель Beget: https://cp.beget.com

2. Откройте настройки поддомена `reklama.ятута.рф`:
   - **Домены** → **Поддомены** → найдите `reklama.ятута.рф`
   - Нажмите на иконку настроек (шестеренка) или название

3. В настройках поддомена найдите:
   - **"Прокси"** или **"Reverse Proxy"** или **"Настройки сайта"**
   - Или **"Тип сайта"** → выберите **"Прокси"** или **"Reverse Proxy"**

4. Настройте прокси:
   - **URL проксирования:** `http://127.0.0.1:5001` или `http://localhost:5001`
   - Или **Порт:** `5001`
   - Сохраните настройки

5. Подождите 1-2 минуты для применения изменений

6. Откройте: `http://reklama.ятута.рф` (без порта!)

---

## Решение 2: Изменить порт бота на 80 (HTTP)

Если нет возможности настроить прокси, можно изменить порт бота на 80:

**Внимание:** Для порта 80 нужны права root (у вас есть) и порт должен быть свободен.

```bash
# На сервере
cd /opt/reklama_bot

# Остановить бота
pkill -f "python3 main.py"

# Изменить порт на 80
sed -i "s/port=5001/port=80/g" main.py

# Запустить заново
screen -S reklama_bot
python3 main.py
# Ctrl+A, затем D
```

Затем откройте: `http://reklama.ятута.рф` (без порта)

---

## Решение 3: Создать .htaccess для проксирования

Если Beget использует Apache, создайте файл `/opt/reklama_bot/.htaccess`:

```apache
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ http://127.0.0.1:5001/$1 [P,L]
```

Но это работает только если поддомен настроен на папку `/opt/reklama_bot`.

---

## Решение 4: Использовать nginx (если доступен)

Если на сервере есть nginx, можно настроить виртуальный хост:

```nginx
server {
    listen 80;
    server_name reklama.ятута.рф;

    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## Рекомендуемый порядок:

1. **Сначала попробуйте Решение 1** - настройка через панель Beget (самое простое)
2. Если не получается - **Решение 2** - изменить порт на 80
3. Если ничего не помогает - обратитесь в поддержку Beget

---

## Проверка после настройки:

1. Откройте: `http://reklama.ятута.рф` (без порта!)
2. Должен открыться веб-интерфейс управления ботом
3. Если не работает - подождите 2-3 минуты и попробуйте снова


