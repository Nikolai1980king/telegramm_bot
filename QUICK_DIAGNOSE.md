# ⚡ Быстрая диагностика: Бот не отправляет сообщения

## 🔧 Выполните эти команды на сервере:

```bash
ssh root@212.67.11.50
cd /opt/reklama_bot

# 1. Проверить процесс
echo "=== ПРОЦЕСС ===" && ps aux | grep "python3 main.py" | grep -v grep

# 2. Посмотреть логи
echo -e "\n=== ЛОГИ (последние 50 строк) ===" && tail -50 bot.log

# 3. Проверить конфиг
echo -e "\n=== КОНФИГ ===" && cat bot_config.json

# 4. Проверить время
echo -e "\n=== ВРЕМЯ СЕРВЕРА ===" && date
```

---

## ✅ После выполнения команд:

**Пришлите вывод** - я помогу найти проблему!

---

## 🎯 Возможные проблемы и решения:

### 1. Бот не запущен
**Симптомы:** `ps aux | grep "python3 main.py"` ничего не показывает

**Решение:**
```bash
cd /opt/reklama_bot
pkill -f "python3 main.py"
nohup python3 main.py > bot.log 2>&1 &
ps aux | grep "python3 main.py"
```

### 2. Бот ждет расписания
**Симптомы:** В логах видно "⏰ Следующая отправка по расписанию..."

**Решение:** 
- Проверьте настройки расписания в конфиге
- Или откройте веб-интерфейс http://212.67.11.50:5001 и измените расписание

### 3. Ошибка авторизации
**Симптомы:** В логах "❌ Требуется авторизация!"

**Решение:** Проверьте наличие session файла:
```bash
ls -lh /opt/reklama_bot/*.session
```

### 4. Файл изображения не найден
**Симптомы:** В логах "⚠️ Файл не найден"

**Решение:** Проверьте путь к изображению в конфиге

---

## 📝 Скопируйте и выполните все команды из блока выше - пришлите результат!

