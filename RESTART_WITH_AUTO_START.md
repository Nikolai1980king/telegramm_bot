# 🔄 Перезапуск с автозапуском бота

## После загрузки обновленного main.py:

Выполните на сервере:

```bash
cd /opt/reklama_bot

# 1. Остановить бот
pkill -9 -f "python3 main.py"

# 2. Подождать
sleep 2

# 3. Перезапустить
nohup python3 main.py > bot.log 2>&1 &

# 4. Подождать и проверить логи
sleep 5
tail -30 bot.log
```

---

## Что изменилось:

Теперь бот автоматически запускается при старте Flask, если в конфиге `is_running: true`.

Вы должны увидеть в логах:
- "🚀 Автозапуск бота (is_running = True)..."
- "✅ Авторизация успешна!"
- "⏳ Следующая отправка через..."

