# 🚀 Запуск бота на сервере

## ✅ Диагностика: бот не запущен

Процесса нет. Запускаем бота!

---

## Выполните на сервере:

### 1. Перейти в папку проекта:

```bash
cd /opt/reklama_bot
```

### 2. Запустить бота через screen:

```bash
screen -S reklama_bot
python3 main.py
```

**Оставьте терминал открытым** - увидите, запустился ли бот и есть ли ошибки.

Затем нажмите **Ctrl+A**, затем **D** (бот продолжит работать в фоне).

---

### 3. Проверить, что запустился:

```bash
ps aux | grep "python3 main.py"
```

Должен появиться процесс.

---

### 4. Проверить порт:

```bash
netstat -tlnp | grep 5001
```

Должен слушаться порт 5001.

---

## Если есть ошибки при запуске:

Посмотрите, что выводится в терминале. Возможные проблемы:

1. **Ошибка импорта** - нужно установить зависимости
2. **Ошибка конфига** - нужно проверить bot_config.json
3. **Ошибка авторизации** - нужно проверить session файл
4. **Порт занят** - нужно изменить порт

---

## После запуска:

Откройте в браузере: `http://212.67.11.50:5001`

Должен открыться веб-интерфейс!

