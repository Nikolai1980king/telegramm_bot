# 🔧 Проблема: Бот показывается запущенным, но потом оказывается остановленным

## Проблема:

1. Вы запускаете бота через веб-интерфейс
2. Видите статус "Работает"
3. Выходите со страницы
4. Возвращаетесь - бот остановлен

## Причина:

Бот падает сразу после запуска (ошибка авторизации, файл не найден и т.д.), но вы успеваете увидеть статус "Работает" до того, как бот упадет. В блоке `finally` бот устанавливает `is_running = False`, поэтому при следующем входе бот показывается остановленным.

## Решение:

Добавить проверку реального состояния потока бота при загрузке страницы и синхронизировать с конфигом.

