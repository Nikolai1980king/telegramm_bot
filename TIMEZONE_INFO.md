# ⏰ Время и часовой пояс бота

## ✅ Как работает время:

**Бот использует системное время сервера**, на котором он запущен.

---

## 📍 Проверка времени на сервере:

На сервере выполните:

```bash
# Посмотреть текущее время
date

# Посмотреть часовой пояс
timedatectl

# Или
cat /etc/timezone
```

---

## 🌍 Важно:

- **Бот работает по времени сервера** (не вашего компьютера!)
- Если сервер в UTC - время будет по UTC
- Если сервер в московском времени - будет по Москве
- При настройке расписания учитывайте время сервера!

---

## ⚙️ Как изменить часовой пояс сервера (если нужно):

### Вариант 1: Установить московское время (MSK)

```bash
# Установить часовой пояс
timedatectl set-timezone Europe/Moscow

# Проверить
date
```

### Вариант 2: Другие часовые пояса:

```bash
# Посмотреть доступные зоны
timedatectl list-timezones | grep Moscow

# Установить
timedatectl set-timezone Europe/Moscow  # Москва
# или
timedatectl set-timezone Asia/Yekaterinburg  # Екатеринбург
```

---

## 🔍 Как проверить, какое время использует бот:

В логах бота вы увидите время отправки, например:
```
🖼️ Отправлено для @user в 12:00 03.12.2025
```

Это время **по часовому поясу сервера**.

---

## 💡 Рекомендации:

1. **Проверьте часовой пояс сервера** перед настройкой расписания
2. **Установите нужный часовой пояс** (например, московское время)
3. **Учитывайте часовой пояс** при настройке времени отправки

---

## 📝 Пример:

Если сервер в UTC (на 3 часа раньше Москвы):
- Вы настроите отправку на 12:00 (по UTC)
- В Москве это будет 15:00

**Решение:** Установите часовой пояс сервера на Europe/Moscow

